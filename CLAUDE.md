# 품(FOOM)

산림복지전문가와 지역주민을 잇는 프로그램 예약 중개 플랫폼. 운영 주체는 체질숲협동조합.

**스택** Firebase Hosting + Cloud Functions(Blaze) + Firestore / React 19 + TypeScript + Vite + Tailwind v4 + shadcn/ui
**외부 연동** 지도=카카오맵 · 결제=토스페이먼츠+포트원 · 날씨=기상청 공공데이터포털 · 알림=카카오 알림톡(+SMS fallback) · 로그인=카카오·네이버 소셜 (본인확인은 **공급자 등록에만**)

---

## 문서 지도

> **작업을 시작하기 전에 아래 표에서 관련 문서를 찾아 반드시 읽으세요.**
>
> 이 파일에는 모든 작업에 공통으로 적용되는 규칙과 현재 상태만 있습니다. **필드명·API 경로·상태값·보안규칙은 여기 없습니다.** 분야별 작업 규칙도 여기 없습니다 (2026-08-31에 분리) — **화면(`src/`) 작업 전에는 `MD/frontend/frontend.md`, 서버(`functions/`)·데이터 작업 전에는 `MD/backend/backend.md`, 인증·권한·쓰기 경로·결제를 건드리기 전에는 `MD/security/security.md`를 반드시 읽으세요.**
>
> - 필드명이나 API 경로를 **추측해서 코드를 쓰지 마세요.** 반드시 해당 문서를 열어 확인한 뒤 작성합니다
> - 스키마 문서는 2,000줄이 넘습니다. 통째로 읽지 말고 **표에 적힌 섹션 번호만 찾아 읽으세요**
> - 작업 도중 판단이 서지 않는 결정 사항이 나오면 "검토 결과 판단 근거"에 왜 그렇게 정했는지가 적혀 있습니다. 추측하기 전에 먼저 확인하세요
> - 문서와 코드가 어긋나면 **문서가 기준**입니다. 코드를 문서에 맞추고, 문서가 틀렸다고 판단되면 임의로 고치지 말고 먼저 알려주세요

| 알고 싶은 것 | 읽을 파일 | 섹션 |
|---|---|---|
| **보안 절대 규칙** (쓰기 경로·권한·결제 금액·본인확인) | `MD/security/security.md` | — |
| **서버 절대 규칙** (검색 API·파생 필드·회차·날씨 캐싱) | `MD/backend/backend.md` | — |
| **화면·디자인 규칙** (토큰·입력칸·아이콘·lint) | `MD/frontend/frontend.md` | — |
| Firestore 필드 정의, 컬렉션 구조 | `MD/backend/백엔드-설계-스키마.md` | 2번 |
| API 엔드포인트 목록 | 〃 | 5번 |
| 보안규칙 전문 | 〃 | 6-1 |
| 보안규칙 테스트 케이스 | 〃 | 6-3 |
| 인덱스 | 〃 | 7번 |
| 관리자 페이지 설계 | 〃 | 12번 |
| 착수 전 필수 보안 조치(P0) | 〃 | 13번 |
| 왜 이 스택인가 (벤더 선정 근거·수수료·바우처) | 〃 | 14번 |
| 로그인·회원가입·본인확인 정책 | 〃 | 15번 |
| 날씨 API 연동 (캐싱·예보 기간) | 〃 | 16번 |
| 검색 필터·기간 선택 (필드·쿼리 전략·캘린더 규칙) | 〃 | 17번 |
| 파일 업로드 (프로그램 사진·자격증, Storage 규칙) | 〃 | 18번 |
| 지도·주소 검색 (카카오 키 4종 구분·지오코딩) | 〃 | 19번 |
| 상세 페이지 구성·소개 양식·사진 비용 | 〃 | 20번 |
| 왜 그렇게 정했는지(의사결정 이력) | 〃 | 0번 + 맨 끝 "검토 결과 판단 근거" |
| 컬렉션 관계 한눈에 보기 | 〃 | 3번 |
| 수수료·환불·정산 관행의 근거, 경쟁 플랫폼 분석 | `MD/planning/벤치마킹-분석-리포트.md` | — |
| 스프린트 일정 | `MD/planning/스프린트-로드맵.md` | — |
| 버그·보안 점검 기록 (8/21 종합 + 8/28 정기) | `MD/security/점검-리포트.md` | — |
| **팀이 보는 개발 현황판 (웹)** | https://foom7707.github.io/foom-board/ | — |

경로는 프로젝트 루트 기준 상대경로입니다. **모든 문서는 `MD/` 폴더에 분야별(frontend·backend·security·planning)로 모여 있습니다** (2026-08-31 정리 — `docs/`는 없앴고, 낡은 html 시안 3종(디자인 초안·구조 다이어그램·상세 시안)은 실제 화면·스키마와 어긋나 삭제했습니다. git 이력에 있습니다). **스키마 문서는 파일명에 버전을 넣지 않습니다** — 버전은 문서 상단 제목과 변경 이력으로만 관리하므로, 버전이 올라가도 위 경로는 그대로입니다. 현재 최신은 v34입니다.

> **개발 현황판은 이 저장소에 없습니다** (2026-08-25). 팀이 보는 화면은 웹 한 곳이고, 고치는 파일도 **프로젝트 밖**(바탕화면 `foom-board` 폴더)에 있습니다 — 저장소가 `FOOM7707/foom-board`로 따로 있어 이 프로젝트와 섞이지 않습니다. 갱신 방법은 아래 「작업이 끝나면 현황판도 갱신합니다」에 있습니다.
>
> **`tools/` 폴더는 지웠습니다** (같은 날, 팀 확정). 같은 현황판이 세 군데에 있어 어긋날 수밖에 없는 구조였고, **웹 하나만 남기기로** 했습니다 — 근거는 「결정 대기 ⑦」. 저장소 안 사본(`tools/개발현황판.html`)과 그 설명은 git 이력(`a11e1ac`)에 남아 있고, 설명은 `foom-board`의 README로 옮겼습니다. **클로드 공유 링크(눌러서 상태를 바꾸는 판)도 더 이상 쓰지 않습니다.**

---

## 절대 규칙

**절대 규칙은 분야별 파일로 옮겼습니다** (2026-08-31). 내용은 한 글자도 줄이지 않았고 자리만 나눴습니다 — **해당 분야를 건드리기 전에 그 파일을 읽지 않고 작업을 시작하지 마세요.**

| 분야 | 파일 | 담긴 절대 규칙 |
|---|---|---|
| 보안 | `MD/security/security.md` | 돈·권한·상태 데이터의 쓰기 경로 / 관리자 API·감사로그 / Custom Claims / 결제 금액은 서버가 결정 / 가입은 소셜 2종뿐 / 소비자 본인확인 없음 / 아동 안전은 예약 폼 / 실명·CI는 `private` 하위 문서 / 보안규칙 고치면 테스트 함께 |
| 백엔드 | `MD/backend/backend.md` | 검색은 `GET /programs/search` 하나 / 파생 필드는 서버 계산·허용목록 / 회차 날짜는 `syncProgramScheduleDates`만 / 공휴일·주말 미제외 / `nextScheduleAt` 인덱스 금지 / 날씨 캐싱 필수 |
| 프론트엔드 | `MD/frontend/frontend.md` | 디자인 토큰 / 공용 입력칸 규격 / `bg-card` 구분 / 이모지 금지 / 「AI로 만든 티」 4종 금지 / 화면 수정 시 `npm run lint` 필수 |

---

## 코드 컨벤션

**리포** `FOOM7707/FOOM` (private)

### 브랜치는 `main`과 `dev` 두 개뿐입니다

**새 브랜치를 만들지 마세요.** 작업 영역이 달라도, 세션이 바뀌어도 만들지 않습니다.

| 브랜치 | 역할 |
|---|---|
| `main` | **원본.** 항상 배포 가능한 상태. 문제가 생기면 여기로 되돌립니다 |
| `dev` | **작업용.** 모든 커밋은 여기에 쌓습니다 |

```
dev 에서 작업 → 커밋 → 푸시 → dev를 main에 병합 → 다음 작업도 같은 dev에서 계속
```

세션을 시작할 때 **`dev`에 있는지 먼저 확인**하고, 아니면 `git checkout dev`로 옮긴 뒤 작업합니다. 이미 `dev`가 있으므로 `checkout -b`(새로 만들기)를 쓰면 안 됩니다.

**왜 두 개인가** — 롤백 기준점이 필요하기 때문입니다. `dev`에서 뭔가 잘못돼도 `main`에 마지막으로 정상 동작하던 상태가 그대로 남아 있어야 되돌릴 수 있습니다. 반대로 브랜치가 셋 이상이 되면 어디에 무엇이 있는지 알 수 없게 됩니다 — 실제로 v14 시점까지 브랜치 3개가 한 줄로 쌓여, 이름과 내용이 서로 맞지 않는 상태가 됐습니다.

**병합은 일반 merge로 합니다. squash·rebase 병합을 쓰지 마세요.** squash로 병합하면 `main`과 `dev`의 커밋 이력이 달라져 두 브랜치가 갈라지고, 다음 병합부터 충돌이 납니다.

**병합 뒤에도 `dev`를 지우지 않습니다.** 같은 `dev`를 계속 씁니다.

큰 실험이라 `main`을 위험하게 만들 것 같으면, 브랜치를 임의로 만들지 말고 **먼저 물어보세요.**

**디자인 토큰·화면 규칙은 `MD/frontend/frontend.md`로 옮겼습니다** (2026-08-31). 색·폰트·입력칸 규격·`bg-card` 구분·이모지 금지·「AI로 만든 티」 금지 형태·대표 이미지 규칙이 거기 있습니다 — **화면을 만지기 전에 반드시 읽으세요.**

**카테고리 명칭은 자격유형 공식명칭 5종을 씁니다.**
숲해설 / 유아숲체험 / 산림치유 / 숲길등산 / 단체·기업
※ `숲치유`, `등산·트레킹` 같은 옛 표기가 코드에 남아 있으면 고쳐주세요.

---

## 현재 상태

**동작하는 것** **화면 전체가 실제 데이터로 돕니다** (v28). `src/mocks/`는 삭제했습니다 — 홈·검색·상세·등록·수정·내 프로그램·전문가 안내·관리자 전부 서버를 씁니다.

> **⚠️ 딱 한 곳이 예외입니다 — 홈의 후기 3건이 지어낸 내용입니다** (2026-08-25 확인). 「김민지 님」 같은 이름과 후기 본문이 화면 코드에 그대로 박혀 있고, 코드 주석에도 「표시광고 문제가 되므로 오픈 전 반드시 제거」로 적혀 있습니다. **팀 판단으로 지금은 그대로 둡니다.** 없애는 방법은 지우는 것이 아니라 **실제 후기가 쌓이면 켜지도록 조건을 거는 것**입니다 — 그래야 나중에 다시 만들지 않습니다.

**로그인은 카카오·네이버 두 가지가 붙어 있습니다** — 둘 다 **실계정 로그인까지 확인**했습니다(네이버 2026-08-14 v14, 카카오 2026-08-19 v19).

> **카카오에서 막히면 두 곳을 먼저 보세요.** ① **Redirect URI는 REST API 키 쪽에 등록**해야 합니다 — 콘솔이 키별로 관리하는데 인가 요청의 `client_id`가 REST API 키입니다. JavaScript 키 화면에 넣으면 `KOE006`이 납니다 ② **Client Secret은 「사용함」과 `.secret.local` 값이 짝이 맞아야** 합니다 — 한쪽만 있으면 `KOE010`입니다. 토큰 교환 실패는 원인이 여러 갈래인데 증상이 같아서, 카카오 오류 코드를 화면까지 올려두었습니다.

> **카카오는 비즈 앱 없이 됩니다.** 비즈 앱이 필요한 건 이메일·전화번호와 간편가입(싱크)이고, 로그인·닉네임·프로필사진은 심사 없이 열립니다. **두 값이 비어도 가입이 그대로 성립**하도록 설계돼 있습니다(2-1·15-4). 대신 알림톡에 쓸 번호는 예약 화면에서 직접 받아야 합니다.
>
> **네이버와 갈라지는 곳이 셋뿐입니다**(15-7) — ① 인가 시작을 서버가 합니다(`GET /auth/social/kakao/start`). 카카오 인가 주소의 `client_id`가 **REST API 키**이고 그 키는 지오코딩에도 쓰여 번들에 두면 쿼터를 도둑맞습니다 ② 토큰 교환에 `redirectUri`가 필요합니다 ③ Client Secret이 **선택**입니다 — 콘솔에서 켜지 않았는데 보내면 거부됩니다
>
> **카카오 회원번호는 숫자로 옵니다.** 그 값이 uid가 되므로 문자열로 옮기고 없으면 실패시킵니다 — 넘어가면 `kakao_undefined`로 **모든 사용자가 한 계정**을 씁니다(`kakao.test.ts`).

> **로그인이 안 될 때 코드부터 의심하지 마세요.** 네이버 앱이 아직 **「개발 중」 상태**라 개발자센터에 **테스트 멤버로 등록된 아이디만** 로그인됩니다. 네이버 **단체·기업 아이디는 불가**하고, **휴대폰 등 다른 기기에서도 불가**합니다(콜백이 `localhost`). 상세는 15-7.
 인가 요청 → 콜백 화면(`/auth/naver/callback`) → `POST /api/auth/social/naver` → Custom Token → `signInWithCustomToken`. 프로필은 서버가 네이버에서 직접 조회하고, 클라이언트는 인가코드와 state만 넘깁니다. 앱 등록 값은 15-7, 가입 규칙은 2-1·2-14.
로컬에서 확인하려면 **`npm run emu`와 `npm run dev`를 함께** 띄우고 `http://localhost:5173`으로 접속합니다(`127.0.0.1` 아님 — 콜백 등록값이 `localhost`).

> **`npm run emu`는 저장된 데이터에서 이어집니다** (v29 신규). `.emulator-data/`를 불러오므로 **로그인·권한·등록한 프로그램·올린 사진이 재시작해도 남습니다.** 이게 없던 동안에는 에뮬레이터를 끌 때마다 소셜 로그인을 다시 하고 `grant-provider`·`grant-admin`을 다시 돌려야 했습니다(하루에 세 번 반복했습니다).
>
> **저장은 자동이 아닙니다 — `npm run emu:save`를 직접 실행합니다.** 에뮬레이터를 켜둔 채로 다른 창에서 돌리면 됩니다. 끄기 전에 한 번 돌리는 것이 규칙입니다.
>
> **⚠️ `--export-on-exit`(자동 저장)를 쓰지 않는 이유가 있습니다.** `firebase emulators:export`는 임시 폴더를 만든 뒤 **이름을 바꾸는** 방식인데, 윈도우에서는 그 폴더를 감시하는 프로세스가 있으면 막힙니다(`EPERM`) — 우리 환경에는 감시자가 둘 있습니다(개발서버가 루트, 함수 에뮬레이터가 `functions/`). 문제는 **CLI가 대상 폴더를 먼저 비우고 이름 바꾸기를 시도**해서, 실패하면 **원래 저장돼 있던 데이터까지 사라진다**는 점입니다. 실제로 한 번 그렇게 날렸습니다. `emu:save`는 이름 바꾸기 대신 **복사**하고, 새 데이터를 확인한 뒤에 기존 것을 바꾸며 직전 상태를 `.emulator-data.bak/`에 남깁니다.
>
> **「Port 8080 is not open ... port taken」으로 안 뜨면 `npm run emu:kill`을 먼저 돌리세요.** 창을 강제로 닫으면 에뮬레이터의 자바·노드 프로세스가 남아 포트를 계속 붙잡습니다 — 창이 없으니 `Ctrl+C`로 끌 방법도 없습니다. 이 명령은 **개발용 포트를 듣고 있는 프로세스만** 정리하고 테스트용 포트(1xxxx)와 개발서버는 건드리지 않습니다.
>
> **`npm --prefix functions run serve`는 저장된 데이터를 불러오지 않습니다.** 백지 상태로 시작하고 싶을 때만 쓰세요.
>
> **`.emulator-data/`는 리포에 올리지 않습니다**(계정·사진이 들어 있습니다). 폴더가 없어 `npm run emu`가 멈추면 `npm --prefix functions run serve`로 한 번 띄워 로그인·권한을 만든 뒤 `npm run emu:save`를 돌리면 만들어집니다.

**프로토타입만 있고 연동 안 된 것**
- 약관 동의 UI — 항목별 체크박스가 없습니다. 서버가 `service`·`privacy`를 동의한 것으로 기록 중이고 마케팅 수신 동의는 받지 않습니다(P1, 2-12)
- ~~날씨~~ — **연동 완료** (v17). 아래 「날씨」 항목 참고
- ~~지도~~ — **카카오맵 연동 완료** (v18). 아래 「지도·주소」 항목 참고
- 위치 — Geolocation·거리 계산은 실제로 동작

**아예 없는 것** 집계 배치(`rebuildSearchIndex`), 결제(웹훅·`confirmed` 전환), 예약 취소·환불, 정산, 예약내역 목록, 공급자 대시보드(있는 건 `/my/programs` 목록뿐). ~~검색 API~~는 v28에서, **예약 홀드(서버)와 마이페이지는 2026-08-24에** 만들어졌습니다 — 아래 「내일 할 일」 참고

**관리자 심사 화면은 실제로 동작합니다** (v15, **v23에서 탭 3개**). `/admin`에 탭 2개 — **전문가 심사**(`GET /admin/providers` → `POST /admin/providers/{id}/approve`)와 **프로그램 심사**(`GET /admin/programs` → `POST /admin/programs/{id}/review`). 이제 `pending_review`에서 `published`로 넘길 수 있습니다.

> **「수정 승인」 탭이 추가됐습니다** (v23). 게시 중인 프로그램의 수정 요청을 **바뀐 항목만 「전 → 후」로** 보여줍니다 — 설명이 수백 자인데 통째로 두 번 보여주면 무엇이 바뀌었는지 못 찾습니다. 비교는 서버가 만들어 내려보냅니다.
>
> **전문가 심사 탭에 「심사 시작」 버튼이 있습니다** (v23). 결과가 아니라 **진행 표시**입니다 — 누르면 신청자 화면의 진행 단계가 「심사 중」으로 올라갑니다. **누르지 않으면 「심사 대기」에 머물러 방치된 것처럼 보입니다.** 이 버튼은 `verified`를 건드리지 않습니다.
>
> **정산 탭은 만들지 않았습니다** — 예약·결제가 없어 정산할 거래 자체가 생기지 않습니다(12-2의 탭 3 중 2개).
>
> **반려는 사유가 필수입니다.** 사유가 공급자에게 그대로 보이므로, 비어 있으면 무엇을 고쳐야 할지 알 수 없어 재제출이 불가능해집니다.
>
> **관리자 메뉴는 헤더에 조건부로 뜹니다.** 판단 기준은 Firestore의 `users.role`이 아니라 **토큰 Custom Claims**입니다 — 보안규칙과 함수 진입부가 보는 값이 그쪽입니다. **권한을 방금 부여했다면 로그아웃 후 다시 로그인**해야 토큰에 반영됩니다. 메뉴를 숨기는 것도 라우트 가드도 보안이 아닙니다(12-3).
>
> **심사 화면에 심사할 내용이 다 보입니다** (v29). 사진 썸네일(누르면 원본이 열림)·포함·불포함·준비물·소개 블록 전부입니다. **v28까지는 사진이 「3장」이라는 숫자로만 나오고 소개·키워드는 아예 없었습니다** — 서버는 내용을 다 내려보내고 있었는데 화면이 쓰지 않아서, 관리자가 제목·가격만 보고 승인 버튼을 누르는 상태였습니다. 「수정 승인」 탭의 「전 → 후」도 같이 고쳤습니다(사진·소개를 글로 요약하면 「사진 1장 → 사진 1장」으로 보입니다).
>
> **직접 입력한 키워드는 눈에 띄게 표시됩니다** — 목록에서 고른 항목과 달리 과장·허위가 들어올 수 있는 자리라 심사의 핵심입니다.
>
> **게시 승인은 `pending_review` 상태에서만 됩니다.** 승인할 때 서버가 파생 필드를 다시 계산하고, `publishedAt`(신규순 정렬 기준)을 **최초 1회만** 채우며, 하위 `schedules`의 `programStatus`를 함께 갱신합니다. 마지막 것을 빠뜨리면 게시해도 검색에 안 잡힙니다.

**프로그램 등록은 실제로 동작합니다** (v14). `POST /programs`(draft 생성) · `GET /programs/{id}` · `GET /programs?mine=1` · `POST /programs/{id}/submit-for-review`. 등록 화면이 서버에 저장하고 `/my/programs`에서 상태를 봅니다. **파생 필드는 서버가 계산**하므로 폼에 입력칸이 없습니다(걷는 거리만 받고 난이도는 자동).

> **등록·수정 폼은 묶음별 카드입니다** (2026-08-26). 대표 사진 / 기본 정보 / 장소와 가격 / 운영 방식 / 프로그램 소개 / 상세 구성 항목 / 참가 조건 일곱 칸이고, 부품은 `src/components/FormCard.tsx` 하나를 씁니다. **입력칸이 한 줄로 쭉 이어지면 어디까지가 한 묶음인지 보이지 않습니다.** 폼 너비는 800px이고 **저장 버튼은 화면 아래에 고정**됩니다 — 폼이 길어서 저장하려면 매번 끝까지 내려가야 했습니다. **입력 항목·검증·저장 방식은 하나도 바뀌지 않았습니다.**
>
> **소개 블록은 끌어서 순서를 바꿉니다** (2026-08-26). 왼쪽 손잡이를 잡고 옮기면 그 자리에 끼워 넣습니다. **위/아래 버튼을 없애지 마세요** — 끌기는 휴대폰에서 동작하지 않고 키보드로도 쓸 수 없어 버튼이 유일한 대안입니다. 손잡이를 누른 동안에만 끌기가 켜지는데, 카드 전체를 늘 끌 수 있게 두면 **글을 선택하려고 문지르는 순간 블록이 끌려갑니다.**

**프로그램을 수정할 수 있습니다** (v22). `내 프로그램`의 각 카드에 「수정」 버튼이 있고, 등록 화면이 값이 채워진 상태로 열립니다. 반려된 프로그램은 「수정해서 다시 제출」로 표시되고, 고쳐 저장하면 자동으로 재심사에 올라갑니다 — **그전에는 반려되면 손을 쓸 방법이 없었습니다.**

> **기본은 재심사, 예외만 즉시 반영입니다.** 즉시 반영은 배리어프리·우천 대체·걷는 거리·문의 가능 기간뿐이고, 나머지(제목·소개·사진·가격·정원·장소·카테고리·자격유형·대상연령)를 고치면 게시 중이던 프로그램이 심사로 돌아갑니다. **날짜(회차)는 재심사를 유발하지 않습니다** — 우천으로 일정을 옮기는 일이 심사에 막히면 안 됩니다.
>
> **게시 중인 프로그램은 고쳐도 게시가 중단되지 않습니다** (v23). 심사 대상 항목은 **수정본으로 따로 보관**되고, 손님에게는 승인된 게시본이 계속 보입니다. 관리자가 승인하면 그 자리에서 교체되고 **게시 상태·최초 게시 시각은 바뀌지 않습니다.** 반려하면 수정본만 버려지고 게시본은 살아 있습니다.
>
> **수정본은 프로그램당 한 개입니다.** 다시 고치면 덮어쓰고, 게시본과 같은 값으로 되돌리면 사라집니다. 전문가는 「수정 요청 취소」로 스스로 버릴 수 있고, 관리자가 프로그램을 숨기면 함께 폐기됩니다.
>
> **수정본은 소유자와 관리자만 읽습니다.** 심사 전 문구·가격이 손님에게 보이면 「게시본만 노출한다」는 전제가 깨집니다 — 보안규칙으로 막고 테스트 7종으로 못박았습니다.
>
> **수정도 반드시 서버를 거칩니다.** 보안규칙은 작성 중 문서의 클라이언트 직접 수정을 허용하지만, 그 길로 가면 지역·난이도가 갱신되지 않아 「주소는 강원도인데 검색에서는 경기도」가 됩니다.
>
> **날짜 관리가 이 화면에 있습니다.** 저장된 회차 목록·삭제(예약 있으면 서버가 거부)·추가가 한자리에 모여 있습니다. **지난 회차는 지울 수 없습니다.**

**상세 페이지가 실제 데이터로 그려집니다** (v27). 사진 슬라이더 · 포함/불포함/준비물 칩 · 소개 블록 지그재그 · 회차 목록 · 카카오맵 · 날씨가 모두 실제 값입니다. **이 화면은 폭이 1440px입니다.**

> **검색 API 없이도 됐습니다** — 상세는 프로그램 하나만 읽으면 되고 `GET /programs/{id}`가 회차·운영자까지 함께 내려줍니다. 홈·검색은 여전히 가짜 데이터입니다.
>
> **운영자 정보는 공개 프로필만 내려옵니다.** 정산 계좌·자격증·심사 사유는 `private` 하위 문서에 있어 이 경로로 오지 않습니다 — 하나라도 섞이면 상세 페이지가 그대로 공개해버립니다.
>
> **하단 고정 「참여하기」와 날짜 선택 서랍이 붙었습니다** (2026-08-24). 날짜 선택까지 실제로 동작하고 결제 자리에만 「준비 중」이 들어 있습니다. 회차 목록의 각 줄에서 「날짜 선택」을 눌러도 그 날짜가 선택된 채 열립니다.
>
> **미게시 프로그램을 소유자·관리자가 열면 상단에 「아직 손님에게는 보이지 않습니다」가 뜹니다** — 손님 화면과 똑같아 보이면 이미 공개된 것으로 오해합니다.

**포함·불포함·준비물과 프로그램 소개를 넣을 수 있습니다** (v26 · **v29에서 사진 방식 변경**). 등록·수정 화면에서 **키워드를 눌러 고르고**, 목록에 없으면 직접 입력합니다(구분마다 3개까지). 소개는 **블록 최대 5개**(**사진 1장** + 소제목 + 설명)로 받고 **배치는 「양식」이 정합니다**.

> **소개 블록 한 칸은 열린 상태로 시작합니다** (2026-08-27, 팀 요청). 「＋ 소개 블록 추가」를 눌러야 칸이 나타나면 **그 버튼을 찾지 못해 소개를 비운 채 넘어갑니다** — 첫 블록이 상세 페이지의 첫 문단이라 비면 상세가 제목·사진만 남습니다. **손대지 않은 빈 칸은 저장할 때 걸러냅니다** — 안 걸러내면 소개를 안 쓰는 사람이 저장할 때마다 「1번째 소개 블록을 채우거나 지우세요」에 막히는데, 그 칸은 본인이 만든 것이 아닙니다. **사진만 있고 글이 없는 칸은 그대로 막습니다**(무슨 사진인지 알 수 없음).
>
> **사진 목록은 하나입니다** (v29). 저장되는 목록은 한 벌이고 상한은 **프로그램당 5장**, **첫 장이 대표**입니다. **소개 블록에 넣는 사진이 곧 상단 앨범 사진**이기 때문입니다 — 예전에는 목록이 둘이라 같은 사진을 두 번 올려야 했습니다.
>
> **「대표 사진」 칸은 1장만 받습니다** (v30, 2026-08-26 팀 요청). 저장 구조는 그대로이고 **어디서 올리느냐만** 바뀌었습니다 — 나머지 사진은 **소개 블록의 「＋ 사진 등록」에서 올립니다.** 상단에서 다섯 장을 먼저 올리게 하면 **「이 사진들을 어디에 쓰는가」가 정해지지 않은 채 쌓입니다.** 소개 블록에서 **이미 올린 사진 중 고르는 길은 그대로** 둡니다(대표 사진을 소개 글에도 쓸 때 다시 올리지 않아야 합니다).
>
> **사진을 지우면 그 사진을 쓰던 소개 블록에서도 함께 빠집니다.** 목록을 합치면서 한 파일을 앨범과 소개 글이 함께 가리키게 됐고, 정리하지 않으면 소개 글에 깨진 이미지가 남습니다. **블록의 글은 지우지 않습니다** — 사진을 지웠다는 이유로 공급자가 쓴 글을 없애면 복구할 방법이 없습니다.
>
> **블록당 사진은 1장입니다** (v29에서 3장 → 1장). 「사진 한 장 + 글 한 덩어리」가 한 칸이고, 3장짜리 배치를 함께 두면 한 화면에 두 가지 배치가 섞여 리듬이 끊깁니다.
>
> **배치 양식은 지금 「양식 1(지그재그)」 하나뿐입니다.** 사진과 글이 좌우로 번갈아 놓이고 사진 비율도 번갈아 갑니다. **등록 화면에 고르는 칸이 없습니다** — 선택지가 하나면 화면만 늘어납니다. 양식 2·3을 더할 자리는 `IntroBlockView.tsx`의 `LAYOUTS`에 한 줄 등록하는 것으로 끝나게 해뒀습니다. 양식 변경은 **재심사를 유발하지 않습니다**(보기 방식이고 내용이 아님).

> **같은 항목을 포함과 불포함에 함께 고르면 막습니다.** 「입장료」가 양쪽 목록에 있어 실제로 그런 선택이 가능한데, 함께 뜨면 손님이 어느 쪽을 믿어야 할지 알 수 없고 그대로 결제되면 분쟁이 됩니다. 화면에서 고르는 즉시 알려주고 서버도 거부합니다.
>
> **소개 글은 심사를 거칩니다.** 게시 중인 프로그램의 소개·키워드를 고치면 수정 승인 대기로 갑니다 — **손님이 이걸 믿고 결제하는 값**이라 사진과 달리 예외를 두지 않았습니다.
>
> **사진은 등록 화면에서 바로 넣습니다** (v29). 소개 블록마다 「＋ 새 사진」이 있고 **거기서 올린 사진이 그 블록에 바로 들어갑니다.** 예전에는 「글 다 쓰고 저장 → 다시 들어와서 사진」 두 번 일이었습니다.
>
> **브라우저가 파일을 들고 있다가 저장할 때 한 번에 올립니다.** 사진이 저장될 자리 이름에 프로그램 번호가 필요한 것은 그대로인데(그 번호는 저장하는 순간 생깁니다), 그 서버 사정을 화면이 물려받지 않게 한 것입니다. **임시 폴더에 먼저 올리는 방식은 택하지 않았습니다** — 저장하지 않고 떠난 파일이 쌓이고 정리 문제가 새로 생깁니다.
>
> **사진 올리기만 실패하면 프로그램은 저장된 상태로 남습니다.** 되돌리면 방금 쓴 글이 전부 사라지기 때문입니다 — 「사진만 올라가지 못했습니다」로 안내하고 수정 화면에서 다시 올리게 합니다.
>
> **등록 화면과 수정 화면의 사진 칸은 서로 다른 부품입니다** — `PendingImagePicker`(등록, 서버를 부르지 않음)와 `ProgramImageUploader`(수정, 고르는 즉시 업로드). 한 부품에 두 모드를 넣으면 매 동작마다 「지금 저장된 상태인가」를 따져야 하고, 한 번 틀리면 **사진이 사라지거나 두 번 올라갑니다.**
>
> **`src/lib/programContent.ts`는 서버 `functions/src/lib/programContent.ts`의 사본입니다.** 목록이 서로 다르면 **화면에서 고른 항목이 저장될 때 거부됩니다** — `sido.ts`와 같은 임시 구조이고 한쪽만 고치면 안 됩니다.

**프로그램 사진을 올릴 수 있습니다** (v25). 수정 화면에서 사진을 고르면 **브라우저가 크기를 줄여** Storage에 직접 올리고, 서버가 검증해 기록합니다. 최대 5장, 첫 장이 대표이고 순서를 바꿀 수 있습니다.

> **~~프로그램을 먼저 저장해야 사진을 올릴 수 있습니다~~** — **v29에서 바뀌었습니다.** 등록 화면에서 사진을 고르면 브라우저가 들고 있다가 저장할 때 함께 올립니다. 사진이 저장될 자리가 프로그램이 만들어진 뒤에 정해지는 것은 그대로지만(18-3), 그 사정을 작성하는 사람이 겪지 않게 했습니다. 저장 완료 화면의 첫 버튼은 이제 **「심사 요청하기」**입니다.
>
> **크기 줄이기를 브라우저에서 합니다** — 긴 변 1600px, 가능하면 WebP. 서버 비용이 들지 않고 5MB 제한도 자동으로 통과합니다. **이걸 빼면 전송 비용이 10배 이상 뜨고 요금 청구로만 드러납니다**(20-6).
>
> **`imageUrls`는 요청 본문으로 받지 않습니다.** 사진은 `POST|PATCH|DELETE /programs/{id}/images` 세 경로로만 바뀝니다 — 본문으로 받으면 외부 URL을 심을 수 있고, **수정할 때 사진이 전부 지워지는 결함**이 있었습니다(v25에서 수정).
>
> **개발용 에뮬레이터에 `storage`를 함께 띄워야 업로드가 됩니다.** `npm run emu`와 `npm --prefix functions run serve` 모두 포함하고 있습니다 — 다른 명령으로 띄웠다면 빠질 수 있습니다.
>
> **썸네일(작은 사진)은 아직 없습니다.** 홈·검색 목록을 실데이터로 붙이는 날 함께 만듭니다 — 지금 만들면 그걸 쓰는 화면이 가짜 데이터라 검증할 수 없습니다. **그날 빠뜨리면 목록이 가장 비싼 화면이 됩니다.**

**진행 날짜(회차)를 등록할 수 있습니다** (v21). 등록 화면에서 「1회성」은 날짜 한 줄, 「회차제」는 여러 줄(추가·삭제)을 받고 **정원은 회차마다 따로** 셉니다. `POST /programs`가 회차를 함께 받고, 등록 뒤에는 `POST /programs/{id}/schedules`·`DELETE /programs/{id}/schedules/{scheduleId}`로 고칩니다. `/my/programs`에 등록한 날짜가 보입니다.

> **「매주 반복」은 등록 화면에 없습니다** (2026-08-27, 팀 요청). 반복 회차를 만드는 서버 경로(`schedule-templates`)가 없어 그전에도 고를 수 없게 막아둔 상태였는데, **눌러도 안 되는 칸이 남아 있으면 「우리가 못 하는 것」으로 읽힙니다.** 회차제로 같은 요일을 여러 줄 추가하면 같은 결과가 됩니다.
>
> **서버는 그대로입니다** — `weekly`로 저장된 옛 프로그램의 심사 요청은 계속 거부되고, 그 프로그램의 수정 화면에는 「회차제로 바꾸세요」 안내가 남습니다. **안내 문구를 지우지 마세요**(지우면 그 화면이 아무 설명 없이 비어 보입니다). 템플릿을 만드는 날 화면과 서버를 함께 엽니다.
>
> **시간 칸은 한 칸입니다 — 오전/오후는 고르고, 시·분은 직접 칩니다** (2026-08-27). 브라우저 기본 시간 칸의 오전/오후 자리가 너무 좁고, 오른쪽 화살표가 바꾸는 값이 초점 위치에 따라 달라져서 **오전을 오후로 바꾸는 것이 실제로 어려웠습니다.**
>
> **기본 칸(`input type="time"`)을 되살리지 마세요.** 그 칸은 오전/오후를 안쪽에 스스로 그리고 **끄는 방법이 없습니다** — 밖으로 꺼내는 순간 「오전 오전 10:00」이 됩니다. 지금은 테두리 하나 안에 목록과 입력칸을 넣어 한 칸으로 보이게 한 것이고, 높이·글자·모서리는 공용 입력칸과 같은 값입니다. 대신 **휴대폰 기기 시간 선택기는 열리지 않습니다**(숫자 자판이 열립니다).
>
> **12시간제 변환은 `src/lib/time.ts` 한 곳에만 둡니다** — 흩어지면 「화면은 오후 2시, 저장은 오전 2시」가 되고 **에러 없이 「엉뚱한 시각」으로만** 나타납니다. `00:00`(오전 12시)·`12:00`(오후 12시)이 둘 다 「12시」로 보이는 자리라 왕복 변환을 테스트로 확인했습니다. **콜론을 안 쳐도 됩니다**(`1030` → 10:30).
>
> **날짜가 0건이면 심사 요청이 막힙니다.** 통과시키면 「검색에는 뜨는데 예약할 날짜가 없는 프로그램」이 게시됩니다. `open`(상시모집)만 예외입니다.
>
> **회차 삭제는 예약이 있으면 거부됩니다.** 예약 기능이 없어 지금은 항상 통과하지만, 붙는 날 검사를 새로 만들지 않도록 미리 넣어뒀습니다.
>
> **한국시간 환산 지점이 하나 더 늘었습니다**(`kstToInstant`). 서버는 UTC라 그냥 쓰면 자정 회차가 전날로 밀리고, **에러 없이 「옆 날짜」로만 나타납니다** — 날씨와 같은 함정이라 테스트로 못박아뒀습니다(`schedules.test.ts`).
>
> **상세 화면에 실제 날짜가 나옵니다** (v27부터).

**날씨는 실제 기상청 예보를 씁니다** (v17 · **v29에서 상세 페이지 전용**). `GET /external/weather` → 격자 변환 → `weatherCache` 3시간 캐싱. mock은 걷어냈습니다.

> **홈 첫 화면에는 날씨가 없습니다** (v29 팀 확정). 홈의 날씨는 「어디 날씨인지」가 정해지지 않습니다 — 위치를 물어보면 **방문 즉시 권한 창**이 뜨고, 안 물어보면 서울 기준값이라 대부분의 방문자에게 틀립니다. **날씨가 필요한 자리는 프로그램 상세**입니다(장소·날짜가 정해져 있어 예약 판단에 실제로 쓰입니다). 홈 전용 표시 형태(`promo`)도 함께 지웠습니다.
>
> **상세의 지역 이름은 동 단위입니다** — 「경기」가 아니라 「수원시 팔달구 화서동」. 등록 화면이 주소를 검색해 지번으로 저장하므로 **API 없이** 저장된 주소에서 뽑습니다(`localityOfAddress`).
>
> **⚠️ 이름만 정밀해지고 예보는 그대로입니다.** 단기예보는 **5km 격자** 단위라 옆 동이 같은 격자면 같은 값입니다 — 화면 문구를 「동별 정밀 예보」로 읽히게 쓰지 마세요.

> **키가 없으면 위젯이 안내 문구로 대체됩니다** — 화면이 깨지지는 않습니다. 키는 `functions/.secret.local`의 `KMA_SERVICE_KEY`이고, **「일반 인증키(Decoding)」를 넣어야 합니다**(Encoding 키를 넣으면 이중 인코딩으로 거부 — 16-4 ①).
>
> **날씨가 안 맞으면 먼저 이 둘을 의심하세요.** ① 격자 변환 — 틀리면 에러 없이 "옆 동네 날씨"가 나옵니다 ② KST 계산 — 서버는 UTC라 그대로 쓰면 "어제 예보"가 나옵니다. 둘 다 테스트로 못박아뒀습니다(`kmaGrid.test.ts`, `weather.test.ts`).
>
> **기상특보(`advisory`)는 아직 항상 null입니다** — 별도 API라 활용신청이 따로 필요합니다. 붙이면 그 필드만 채워지고 화면 코드는 그대로입니다.
>
> **캐시 자동 삭제는 콘솔에서 TTL 정책을 1회 켜야** 동작합니다(16-4). 안 켜도 서비스는 정상이고 문서만 쌓입니다.
>
> **좌표 문제는 해소됐습니다** (v18). 등록 화면이 주소를 검색해 좌표와 함께 저장합니다. 다만 **v18 이전에 등록된 프로그램은 좌표가 비어 있고 자동으로 안 채워집니다** — 백필이 필요합니다(19-6).

**지도·주소는 카카오를 씁니다** (v18). 지도는 카카오맵, 등록 화면의 주소는 `GET /external/kakao-map/search`로 **검색해서 고릅니다** — 주소와 좌표가 한 쌍으로 저장돼야 상세에 날씨가 붙습니다(19-2).

> **키를 두 개 쓰고, 어느 쪽인지 반드시 구분해야 합니다**(19-1). **JavaScript 키**는 프론트(`.env`의 `VITE_KAKAO_MAP_KEY`) — 번들에 그대로 들어가는 게 정상이고 방어선은 콘솔의 **「JavaScript SDK 도메인」 등록**입니다. **REST API 키**는 서버(`functions/.secret.local`의 `KAKAO_REST_API_KEY`) — 도메인 제한이 없어 프론트에 두면 남이 우리 쿼터를 씁니다. **어드민 키는 앱 전권이라 어디에도 쓰지 않습니다.**
>
> **지도가 안 뜨면 도메인 등록부터 확인하세요.** 키가 틀린 경우와 도메인 미등록은 **응답이 같아 구분되지 않습니다.** `127.0.0.1`이 아니라 `http://localhost:5173`으로 접속해야 합니다(네이버 콜백과 같은 함정).
>
> **줌 단위가 Leaflet과 반대입니다** — 카카오는 숫자가 **작을수록** 확대(level 1 = 20m, 14 = 전국). 옛 zoom 값을 그대로 옮기면 전국을 보여주려다 골목이 뜹니다(19-5).
>
> **좌표는 `x`가 경도, `y`가 위도입니다.** 뒤집어 읽어도 에러가 안 나고 "엉뚱한 위치"로만 나타나서, 한반도 범위 밖이면 결과에서 빼도록 해놨습니다(`placeSearch.test.ts`).
>
> **키가 없으면 지도 자리에 안내 문구가 뜹니다** — 화면이 깨지지 않습니다.
>
> **지도 조작은 19-5에 정리돼 있습니다.** 휠 확대, 확대·축소·내 위치 버튼, 핀을 누르면 뜨는 카드(에어비앤비식 `CustomOverlay` 오버레이), 카드가 잘리지 않게 중심을 옮기는 계산까지 — **화면을 만지다 나온 결함 5건을 고친 결과라 이유가 함께 적혀 있습니다.** 카드 생김새를 바꾸면 `ProgramMap.tsx`의 `CARD_HEIGHT_PX`도 같이 손봐야 합니다.

**전문가 등록은 안내 화면만 있습니다** (`/provider/apply`, v15 · **v20에서 랜딩형 개편** · **v23에서 심사 진행 단계 추가** · **2026-08-24에 진행 단계를 마이페이지로 이동**).

> **심사 진행 단계 4칸은 이제 마이페이지에 있습니다** (2026-08-24). 「접수 → 심사 대기 → 심사 중 → 승인/반려」가 **실제 데이터로 움직이는 것은 그대로**이고 자리만 옮겼습니다 — `/provider/apply`는 **아직 전문가가 아닌 사람을 설득하는 자리**인데, 이미 등록한 사람만 쓰는 정보가 랜딩 맨 아래를 차지하고 있었습니다. 부품(`ReviewProgress.tsx`)은 그대로 씁니다.
>
> **「심사 중」은 관리자가 「심사 시작」을 눌러야 켜집니다.** 데이터 없이 칸만 그리면 그 칸이 영원히 안 켜져 멈춘 것으로 읽힙니다. **온라인 신청 폼은 여전히 없어서** 1번 칸은 「문의로 접수돼 계정이 만들어진 뒤」에 켜집니다.
>
> **신청 방법 안내는 `/provider/apply`에 남겼습니다.** 온라인 폼이 없어 문의가 유일한 시작점이고, 그걸 빼면 이 화면에 다음 행동이 사라집니다. 이미 공급자인 사람에게는 「마이페이지에서 심사 상태 보기」로 안내합니다.

 신청 폼이 아니라 절차 안내입니다 — 정식 절차의 첫 단계인 휴대폰 본인확인 벤더가 계약 전이고 자격증 이미지를 올릴 Storage도 없어서, 폼을 만들면 "관리자가 무엇을 보고 승인하는가"가 정해지지 않은 채 필드부터 정하게 됩니다(15-9). 자격 부여는 그동안 스크립트가 계속 담당합니다.

> **화면을 손볼 때 15-9를 먼저 읽으세요.** 시안을 그대로 가져오지 않은 판단이 넷 적혀 있습니다 — 강조색은 브랜드 그린으로 / **누를 데 없는 CTA·손가락 커서를 두지 않음** / 보증 문구는 실제 저장 방식대로 / 회전은 Tailwind `animate-spin`(인라인 `animation: spin`은 keyframes가 안 생겨 조용히 멈춤).
>
> **이 화면과 프로그램 상세가 폭 1440px입니다**(나머지는 1152px). 큰 사진과 큰 글자가 설득의 수단이라 좁히면 구성이 성립하지 않습니다 — 통일하려고 줄이지 마세요. 절차 4칸의 문장은 **길이를 20~24자로 맞춥니다**(줄 수가 다르면 네 칸이 한 덩어리로 안 보입니다).

> **⚠️ 공급자 자격은 임시 경로로 부여합니다** — `npm run grant-provider -- --uid <uid>` (루트에서 실행. 실제 스크립트는 `functions/`에 있고 루트 명령이 넘겨줍니다). 본인확인 벤더 계약 전이라 15-1의 정식 절차를 만들 수 없어서 둔 우회로이고, **정식 오픈 전 반드시 제거**해야 합니다(15-8, 13번 P1). 이 스크립트로 만든 계정은 `approvalStatus='pending'`·`verified=false`로 남으니 그 값으로 찾아 재심사하면 됩니다 — `/admin`의 전문가 심사 탭에 그대로 뜹니다.

## 내일 할 일 (2026-08-24 갱신)

**8/20에 적었던 3개가 2026-08-24에 전부 끝났습니다** (날짜 선택 서랍 · 예약 홀드 서버 · 기간 필터). **마이페이지도 같은 날 만들었습니다** — 아래 「마이페이지」 항목 참고. 다음 후보는 아래 「언제든 할 수 있지만 아직 안 급한 것」입니다.

**마이페이지가 있습니다** (`/my`, 2026-08-24). **왼쪽 사이드바(LNB) + 오른쪽 본문** 구조이고, 메뉴를 누르면 화면을 다시 불러오지 않고 본문만 바뀝니다. 헤더의 「마이페이지」는 **로그인한 사람에게만** 보입니다 — 비로그인 상태로 눌러도 「로그인이 필요합니다」만 나오므로 헛걸음이 됩니다.

> **메뉴는 계정에 따라 만들어집니다.** 내 정보·예약내역은 모든 회원, **전문가 활동은 전문가 계정에만**, 관리자는 **토큰 클레임에 `admin`이 있을 때만** 생깁니다. 해당 없는 메뉴가 늘어나면 정작 쓰는 것을 찾기 어려워집니다 — 다만 **메뉴를 숨기는 것은 보안이 아닙니다**(12-3).
>
> **선택한 메뉴는 주소에 남습니다**(`/my?tab=provider`). 검색 필터를 URL에 두는 것과 같은 이유이고(17-4 ⑤), 덕분에 **다른 화면에서 특정 탭으로 바로 보낼 수 있습니다** — `/provider/apply`의 「심사 상태 보기」가 그렇게 씁니다. **`replace`로 바꿔서** 뒤로가기가 탭 이동 기록에 갇히지 않게 했습니다(탭을 세 번 누른 뒤 뒤로 가려는 사람은 마이페이지를 벗어나려는 것입니다). 이 계정에 없는 탭이나 오타를 요청하면 **첫 메뉴로 떨어지고 주소도 정리**합니다 — 권한이 없어진 뒤 옛 주소를 열었을 때 빈 화면이 되지 않게 합니다.
>
> **서버 호출은 마이페이지 한 곳(`useMe`)에서만 합니다.** 각 메뉴 컴포넌트는 받은 값만 그리고, 저장 후에는 `reload()`로 다시 읽습니다 — 화면이 상태를 따로 조립하면 서버와 어긋납니다.
>
> **파일은 메뉴별로 갈라놨습니다** — `src/pages/MyPage.tsx`(레이아웃·사이드바)와 `src/pages/my/{Profile,Bookings,Provider,Admin}Section.tsx`. 메뉴를 더할 때는 `MyPage.tsx`의 `buildMenu`에 한 줄 등록하는 것으로 끝나게 해뒀습니다.
>
> **⚠️ 휴대폰 화면은 아직 다듬지 않았습니다.** 좁은 화면에서 사이드바가 위쪽 가로 줄로 접히게만 해뒀습니다(200px 고정 열을 그대로 두면 본문이 손가락 두 개 너비가 됩니다). **최적화는 별도 작업으로 남아 있습니다.**

> **심사 진행 단계는 `/provider/apply`에서 옮겨온 것입니다** (같은 날). 「내가 지금 어디쯤인지」는 내 계정 화면에서 볼 일입니다 — 자세한 이유는 위 「전문가 등록」 항목에 적었습니다.
>
> **상태별로 「그래서 지금 뭘 할 수 있나」를 함께 적습니다.** 특히 심사 대기·심사 중에도 **프로그램 등록과 심사 요청은 됩니다** — 안 적어두면 자격 심사가 끝날 때까지 기다려야 하는 것으로 읽힙니다.

> **수정도 서버를 거칩니다**(`PATCH /users/me`). 보안규칙은 본인의 `users` 문서 직접 수정을 허용하지만(`role`·`status`만 막음), 그 길로 가면 **연락처가 정규화를 건너뜁니다** — 저장은 `+8210…` 한 형식이어야 하고(2-1) 관문이 서버에만 있어서, 형식이 섞이면 같은 번호가 다른 문자열로 남아 중복 감지가 통째로 무력해집니다(15-4). 프로그램 수정에서 한 것과 같은 판단입니다.
>
> **연락처를 바꾸면 번호 선점(`phoneIndex`)도 함께 옮깁니다.** 예전 번호를 풀어주지 않으면 본인이 되돌릴 수 없고 그 번호를 실제로 쓰는 사람도 못 씁니다.
>
> **⚠️ 남이 쓰는 번호는 여기서 거부합니다 — 2-14와 다른 판단이라 확인이 필요합니다.** 2-14는 「선점 실패는 막지 않고 기록만 한다」인데, 그 근거는 「번호가 첫 예약 화면에서 들어오는 사용자가 되돌아갈 곳 없이 멈춘다」입니다. 마이페이지는 그 상황이 아니고(설정 화면이라 그냥 두고 나갈 수 있음), 반대로 조용히 저장하지 않으면 「저장했다는데 안 바뀌는」 고장으로 읽힙니다. **문서가 다루지 않는 경우라 임의로 판단한 것이므로, 팀 확인 후 2-14에 반영하거나 되돌려야 합니다.**
>
> **이름은 빈 값을 거부합니다** — 후기 작성자명 등으로 남에게 그대로 보이는 값이라(2-1) 비면 화면에 빈칸이 남습니다. 가입 시점의 「이용자1234」 폴백은 값이 없을 때 만드는 것이고 일부러 비우는 길이 아닙니다.
>
> **예약내역은 자리만 있습니다.** 예약 홀드(서버)는 만들어졌지만 결제가 없어 쌓이는 예약이 없습니다 — 목록을 만들면 언제나 비어 있어 고장으로 읽힙니다. 헤더의 「예약내역」 메뉴도 「준비중」으로 남겨뒀습니다.

**~~1순위 — 날짜 선택 서랍 + 하단 「참여하기」~~ — 완료 (2026-08-24).**
상세 페이지에 하단 고정 바(가격 + 「참여하기」)와 날짜 선택 서랍(`BookingDrawer.tsx`)이 붙었습니다. 회차 목록의 「예약 준비 중」 글자는 「날짜 선택」 버튼이 되어, 누르면 그 회차가 선택된 채 서랍이 열립니다. **날짜를 고르는 것까지 실제로 동작**하고 결제 자리에만 「준비 중」 버튼이 들어 있습니다 — 예약이 붙는 날 그대로 씁니다(20-5). 상시모집은 협의 안내가 뜨고, 고를 날짜가 없으면 버튼이 비활성으로 이유를 적습니다.

**~~1순위 — 예약 홀드~~ — 서버 완료 (2026-08-24).**
`POST /bookings`가 2-5 플로우 그대로 돕니다 — 정원 확인과 차감이 **한 트랜잭션**(마지막 자리를 동시에 잡으면 하나만 성공하는 것을 테스트로 못박음), `pending_payment`+`expiresAt`(10분), 가격은 서버가 `programs.price`를 읽어 스냅샷(`unitPrice`·`totalAmount` — 클라이언트가 보낸 금액은 아예 읽지 않음). **참가자 검증(15-6)도 같은 트랜잭션에서** 합니다 — 인원=참가자 수, 비상연락처 필수(E.164 정규화), 아동 대상이면 전원 생년월일+보호자 필수, 만 나이는 **회차 진행일 기준**, 연령 제한이 없어도 14세 미만이 있으면 보호자 필수 승격. 판정에 쓴 만 나이는 예약 문서에 기록해 분쟁 시 재계산 없이 답합니다.

> **`releaseExpiredHolds`(5분 주기 스케줄 함수)가 만료 홀드를 `expired`로 바꾸고 정원을 복구합니다.** 건별 트랜잭션이라 **웹훅과 경합해도 `confirmed`를 덮지 않습니다**(트랜잭션 안에서 상태 재확인). ⚠️ 에뮬레이터는 스케줄 함수를 시계로 돌리지 않습니다 — 로컬 확인은 테스트가 담당합니다(`bookings.test.ts` 13종).
>
> **상시모집(`open`)은 아직 받지 않습니다.** 협의(`negotiate`)·1:1 문의 API가 없어 홀드를 만들어도 날짜를 정할 길이 없습니다 — 그 API들과 함께 엽니다.
>
> **화면은 아직 이 API를 부르지 않습니다.** 결제가 없어 지금 홀드를 만들면 「10분 내 결제」 안내만 받고 만료가 반복됩니다 — 예약 폼(참가자 입력)과 결제창은 결제 벤더 계약 후 날짜 선택 서랍에 이어 붙입니다.
>
> **`firestore.indexes.json`이 새로 생겼습니다** (스키마 7번의 `bookings` 인덱스 3종). 배포 시 `firebase deploy --only firestore:indexes`를 한 번 돌려야 합니다 — 에뮬레이터는 인덱스 없이도 돌아서 **빠뜨리면 배포 후에야 드러납니다.**

**~~3순위 — 기간(날짜) 필터~~ — 완료 (2026-08-24).**
검색 필터 모달의 「준비 중」 자리에 달력이 들어갔습니다(`DateRangeCalendar.tsx` — 2개월 나란히, 오늘~+90일, 연속 최대 31일). 판정은 서버가 합니다 — `GET /programs/search`가 `from`/`to`를 받아 `scheduleDates` 교집합으로 거르고, **서버에서도 오늘~+90일·31일로 잘라냅니다**(17-6). 상시모집은 문의 가능 기간이 겹치면 함께 나오고 목록 위에 「기간과 무관하게 표시」 안내가 뜹니다(17-2). 선택한 기간은 URL(`from`/`to`)에 남아 뒤로가기·링크 공유가 됩니다.

> **달력의 「회차 있는 날」 점은 임시 출처를 씁니다.** 설계상 출처는 `aggregates/scheduleCalendar`(17-5)인데 그 배치(`rebuildSearchIndex`)가 아직 없어, 검색 응답이 `calendarDates`(전체 게시 프로그램의 회차 날짜 합집합, 지난 날짜 제외)를 함께 내려줍니다 — 검색 함수가 어차피 게시 프로그램 전체를 읽는 지금 구조에서는 공짜입니다. **집계 배치를 만드는 날 출처만 그쪽으로 바꿉니다.**

**언제든 할 수 있지만 아직 안 급한 것**
- **매주 반복 템플릿** — **화면 선택지에서 빼둔 상태**(2026-08-27)이고 서버 경로도 없습니다. 「회차제로 여러 줄 추가」가 대체 수단이라 막고 있는 것이 없습니다 — 만드는 날 화면·서버를 함께 엽니다
- **목록⇄지도 합치기** — 17-4 ①에서 합치기로 확정했는데 아직 별도 토글입니다
- **자격증 업로드·열람** — **IAM 권한 부여가 먼저입니다**(18-6). 코드로는 해결되지 않으니 그날 그것부터 확인해야 합니다
- **미참조 사진 파일 정리** — 작성 중 프로그램에 사진만 올리고 떠나면 파일이 남습니다(18-7)

**오픈 전에 반드시 해야 하는 것 (잊으면 사고나 비용으로 드러남)**
- **목록용 썸네일** — 게시 프로그램이 20건을 넘거나 오픈 전. 카드 20장이면 5MB가 나갑니다(20-6)
- **`grant-provider` 임시 경로 제거** — 본인확인 벤더 계약 후(15-8, 13번 P1)
- **게시 중 프로그램의 사진 교체를 수정 승인 대기열에 올리기** — 지금은 사진만 심사를 건너뜁니다(코드에 TODO)
- **배포 시 카카오·네이버 콘솔에 실제 도메인 등록** — 지금은 `localhost:5173`만 등록돼 있어 로그인·지도가 막힙니다

---

**다음 작업 순서 (2026-08-19 시점 권고 — 위 「내일 할 일」이 최신입니다)**

**우선순위 기준은 "무엇이 다음 작업을 막고 있는가"입니다.** 사용자가 없으니 기능을 만들 이유가 없다는 지적이 맞고, 그래서 **다음 단계를 막고 있는 것부터** 합니다.

0. ~~카카오맵 연결~~ — **완료** (v18, 2026-08-19). 지오코딩과 지도 교체가 함께 풀렸습니다
1. ~~**회차(날짜) 기능**~~ — **1단계 완료** (v21, 2026-08-20). 날짜 직접 입력(1회성·회차제)과 추가·삭제까지. **매주 반복 템플릿은 남았습니다**(2026-08-27에 화면 선택지에서 제거)
2. **예약 홀드** — **이제 이게 1순위입니다.** 가리킬 날짜가 생겼으니 예약을 만들 수 있습니다. 결제 벤더 계약과 **무관하게** 만들 수 있는 부분입니다(정원 트랜잭션 차감·`expiresAt`·`releaseExpiredHolds`). 계약이 늦어도 9월 스프린트가 비지 않게 미리 당겨둘 수 있습니다
3. ~~**Storage 업로드**~~ — **프로그램 사진 완료** (v25, 2026-08-20). 남은 것은 **자격증 업로드·열람**인데, 서명 URL에 IAM 권한이 필요해 별도 작업입니다(18-6). 이전 서술: 상세 페이지에서 사진이 가장 큰 자리를 차지하는데 올릴 방법이 없어, **상세 페이지 작업 전체를 막고 있습니다.** 규칙·테스트는 끝났고 업로드 UI·서버 연동만 남았습니다. **리사이즈(가로 1600px·썸네일)와 지연 로딩을 이때 반드시 함께** 넣습니다 — 빠뜨리면 전송 비용이 10배 이상 뛰고 요금 청구로만 드러납니다(20-6)
4. ~~**검색 연동**~~ — **완료** (v28, 2026-08-20). 집계 배치(`rebuildSearchIndex`)는 **아직 없습니다** — 게시 프로그램이 500건에 가까워지면 도입할 시점입니다

> 홈·검색·상세가 **모두 실데이터입니다** (v28). 등록 → 승인 → 게시하면 그 결과가 검색 목록과 상세 페이지에 바로 나타납니다.
>
> **단, 홈 카테고리 카드 사진은 프로그램과 무관합니다** (v29 팀 확정). 그 자리는 **우리가 고정으로 넣는 사진**입니다 — 브랜드 첫인상이라 누가 무엇을 올리느냐에 따라 바뀌면 안 됩니다. `public/home/`에 아래 이름으로 파일을 넣으면 그 파일이 쓰이고, 없으면 임시 사진이 나옵니다(코드 수정 불필요).
>
> `forest-guide.jpg`(숲해설) · `kids-forest.jpg`(유아숲체험) · `healing.jpg`(산림치유) · `trail.jpg`(숲길등산) · `group.jpg`(단체·기업) — **비율 3:4 세로**. 지금은 Unsplash 임시 사진이라 **오픈 전 교체 대상**입니다.
>
> **일정 리스크는 코드가 아니라 계약입니다.** 결제(토스페이먼츠·포트원)가 Sprint 3(9/14~) 선행 조건이고 약 4주 남았습니다. 본인확인(KG이니시스)은 전문가 등록 정식 절차를 계속 막고 있습니다.

### 결정 대기

**① ~~공급자 등록을 어떻게 열 것인가~~ — 확정 (2026-08-18).** 안내 화면만 만들었습니다(위 A안, 15-9). 신청 기능은 본인확인 벤더 계약과 Storage 설정이 끝난 뒤에 다시 논의합니다.

**② 지역 필터 권역이 7개인가 8개인가.** 17-3은 「8개 권역」이라 적혀 있는데 4번 매핑표는 7개(서울/경기·인천/강원/충청/전라/경상/제주)입니다. **17개 시도를 빠짐없이 덮고 있어 기능 영향은 없고** 숫자만 안 맞습니다. 미결정 — 임의로 고치지 마세요.

**③ `MD/planning/스프린트-로드맵.md`의 착수주 항목에 「문자인증」이 남아 있습니다.** 15-1에서 폐기된 설계입니다. 로드맵은 8/7 작성본이라 그 뒤 결정이 반영돼 있지 않습니다 — 문서가 기준이라 임의로 고치지 않았습니다. 정리 여부 미결정.

**⑥ 자격 승인 전에 「프로그램 등록」·「내 프로그램」을 보여줄 것인가** (2026-08-24 제기, 미결정).

팀 의견은 **「승인이 떨어진 뒤에만 보여주자」**입니다 — 승인도 안 났는데 등록 버튼이 떠 있으면 「지금 이걸 해도 되나」가 됩니다. **지금 코드는 보여주는 쪽이고, 그게 문서에 정해진 설계라 임의로 바꾸지 않았습니다.**

- **지금 왜 보여주는가** — 서버가 승인 전 등록을 허용합니다(`assertProvider`가 `role`·`status`만 보고 `approvalStatus`는 보지 않음). 의도된 설계이고 화면 문구에도 「심사 대기 중에도 프로그램 등록과 심사 요청은 할 수 있습니다」로 적혀 있습니다.
- **근거는 심사가 둘이라는 것** — **자격 심사**(전문가 본인, 1회)와 **게시 심사**(프로그램마다)가 별개입니다. 자격 심사를 기다리는 동안 프로그램을 써두면 승인되는 날 바로 게시 심사를 요청할 수 있습니다. 감추면 「자격 대기 → 그 다음 작성 → 또 게시 대기」가 되어 **첫 손님을 받기까지의 시간이 두 배**가 됩니다. 승인 메일을 받고 들어온 사람이 **처음으로** 버튼을 보게 되는 문제도 있습니다.
- **권고안** — 버튼은 남기고 **승인 전후에 무게 차이만** 줍니다(승인 전 테두리 버튼 → 승인 후 채운 버튼). 「할 수 있다」는 사실은 유지하고 「지금이 본격적으로 할 때」라는 신호만 더합니다.
- **감추기로 정하면 함께 해야 할 것** — ① **서버도 막아야 합니다**(화면만 감추면 주소를 직접 쳐서 등록됩니다 → 화면과 서버가 다른 말을 하는 상태) ② 스키마 15-1의 관련 서술과 상태별 안내 문구도 함께 고칩니다.

**⑦ 현황판 사본 세 개를 어떻게 정리할 것인가 — 확정 (2026-08-25).** **웹 하나만 남깁니다.**

같은 현황판이 세 군데에 있었습니다 — ① **웹**(https://foom7707.github.io/foom-board/) ② `tools/개발현황판.html`(저장소 안 사본) ③ **클로드 공유 링크**(팀원이 상태를 눌러 바꿀 수 있는 판). **어느 것도 서로 자동으로 따라 바뀌지 않아** 고칠 때마다 어긋날 수밖에 없는 구조였습니다.

**②는 지웠고, ③은 쓰지 않습니다.** ②는 웹이 생기기 전 「내려받아 보는 용도」라 역할이 끝났습니다. ③의 「눌러서 바꾸기」는 편하지만 **그 결과가 정작 팀이 보는 화면(①)에 반영되지 않아** 오히려 어긋남의 원인이 됩니다 — 팀원이 상태를 바꿔놓고 「반영했다」고 여기는데 팀장님 화면은 그대로인 상황이 생깁니다.

- **②는 git 이력에 남아 있습니다**(`a11e1ac`에서 추가된 뒤 이 커밋에서 지워짐). 되살릴 일이 있으면 거기서 꺼냅니다. 설명 문서는 `foom-board`의 README로 옮겼습니다
- **③의 링크가 이미 팀에 공유돼 있다면 웹 주소로 안내를 바꿔야 합니다** — 옛 링크를 계속 보는 사람이 있으면 두 화면이 갈라집니다
- **앞으로 현황판 사본을 새로 만들지 마세요.** 내려받기·복사본·다른 문서에 옮겨 붙이기 전부 같은 문제를 다시 만듭니다

> **함께 정해진 것(반영 완료)** — 「전문가 활동」 칸은 **전문가 계정에만** 보입니다(일반 회원에게는 칸 자체가 없음). 기준은 `role === 'provider'`이고 **「승인 완료」가 아닙니다** — 승인 후에만 보이면 본인이 「심사 대기」·「심사 중」 칸을 영원히 못 보게 되어 진행 단계 4칸을 만든 이유가 사라집니다(v23). 정식 신청 폼이 생기면 **「본인확인 + 서류 제출 완료」가 곧 `role === 'provider'`가 되는 시점**이라, 그때 이 기준을 다시 손댈 필요가 없습니다.

**④ ~~게시 후 수정을 어떻게 노출할 것인가~~ — 확정 (2026-08-20).** **수정본을 따로 보관하고 게시본은 그대로 노출하다가 승인 시 교체**하는 방식으로 구현했습니다(v23). 오늘의집 파트너센터의 항목별 승인 구분을 참고했고, 사후검수(먼저 노출하고 문제 시 복원)는 **택하지 않았습니다** — 관리자가 한 명이라 상시 모니터링 인력이 없고, 잘못된 정보를 보고 결제한 예약은 되돌리기 어렵습니다. **우천 대응의 본체(예약자가 있는 회차의 일정 변경)는 예약 기능과 함께** 설계합니다 — 손님 동의 없이 옮기면 사실상 취소이고, 벤치마킹 리포트 4.3에 「날씨 취소는 조사한 5개 플랫폼 모두 규정이 없어 자체 설계 필요」로 남아 있습니다. 이전 서술: 지금은 심사 대상 필드를 고치면 **게시가 내려가고 승인까지 검색에서 사라집니다.** 이러면 전문가가 오타조차 고치지 않게 되어 정보 품질이 오히려 나빠집니다. 검토 중인 대안은 **수정본을 따로 보관하고, 손님에게는 승인된 게시본을 계속 보여주다가 승인 시 교체**하는 방식입니다(관리자 화면에 「전 → 후」 비교 필요). 오늘의집 파트너센터가 항목별 승인 구분 + 사후검수(문제 시 변경 전으로 복원)로 같은 문제를 다루고 있어 참고했습니다. **우천 대응의 본체(예약자가 있는 회차의 일정 변경)는 예약 기능과 함께** 설계합니다 — 손님 동의 없이 옮기면 사실상 취소이고, 벤치마킹 리포트 4.3에 「날씨 취소는 조사한 5개 플랫폼 모두 규정이 없어 자체 설계 필요」로 남아 있습니다.

**⑤ 상세 페이지 구성과 소개 양식 — 확정 (2026-08-20). 설계만 끝났고 구현은 대기 중입니다.** 상세는 20번에 정리했습니다. 요점만 옮기면 —

- **상세 소개를 「이미지 한 장」으로 받지 않습니다.** 국내 관행은 공급자가 상세페이지를 이미지로 만들어 올리는 것이지만, 우리 공급자는 디자이너가 없는 개인 전문가이고, 무엇보다 **「바뀐 항목만 전 → 후로 보여주는」 수정 승인이 이미지 앞에서는 무력화**됩니다. 에어비앤비 체험도 정해진 양식만 받습니다
- **사진에 글자를 넣게 하지 않습니다.** 공급자는 사진을 고르고 글을 쓸 뿐, 배치는 우리가 합니다
- **소개 블록 최대 5개** = **사진 1장**(v29 개정) + 소제목(30자) + 설명(300자). **첫 블록만 필수.** **배치는 양식이 정합니다** — 지금은 양식 1(지그재그)뿐이고, 사진과 글이 좌우로 번갈아 놓입니다
- **대표 사진 최대 5장** (상단 슬라이더·목록 카드), 첫 장이 대표. 1장뿐이면 슬라이더 없이 한 장
- **포함·불포함·준비물은 목록에서 고르고 직접 입력 3개까지.** 직접 입력분은 **심사 대상**입니다. **「입장료」가 포함·불포함 양쪽 목록에 있어 서버에서 교차 검사로 모순을 막습니다**
- **시안의 할인율·케어 배너·찜·후기 수·가격 옵션은 넣지 않습니다** — 없는 기능을 약속하게 됩니다(`/provider/apply`에서 같은 문제가 이미 있습니다)
- **날씨 예보는 유지**합니다(시안에 없지만 우리에게 있습니다)
- **하단 「참여하기」는 날짜 선택까지만** 동작합니다. 회차 데이터가 있어 실제로 동작하고, 예약이 붙을 때 그대로 씁니다
- **이 화면도 폭 1440px입니다** — 아래 「전문가 안내 화면만 1440px」 서술은 상세 구현 시 함께 고쳐야 합니다
- **비용은 양식이 아니라 리사이즈가 정합니다.** 사진 원본을 그대로 내려보내면 같은 방문 수에 10배 이상 차이가 납니다

### 대기 중인 외부 항목 (2026-08-19 기준)

| 항목 | 상태 | 없으면 막히는 것 |
|---|---|---|
| 기상특보 활용신청 | 미신청 | 주의보·경보 표시(`advisory` 항상 null) |
| Firestore TTL 정책 | 콘솔 미설정 | 날씨 캐시 자동 삭제(안 켜도 정상 동작, 문서만 쌓임) |
| 카카오 비즈앱 심사 | 미신청(대표자 본인인증 필요) | 카카오에서 **이메일·전화번호**를 못 받음 · 간편가입(싱크) — **로그인 자체는 됩니다** |
| 본인확인 벤더(KG이니시스) | 계약 전 | 전문가 등록 정식 절차 · `grant-provider` 우회로 제거 |
| 결제(토스페이먼츠·포트원) | 계약 전 | **Sprint 3(9/14~) 전체** |

### 알려진 문제

- ~~상세 화면이 `src/mocks/`를 씁니다~~ — **v27에서 실데이터로 전환 완료.** 남은 것은 홈·검색이고 그쪽은 검색 API가 필요합니다 v21에서 실제 회차가 생겼는데 이 화면은 가짜 날짜를 보여줍니다 — 서버(`GET /programs/{id}`)가 회차를 함께 내려주므로 이 화면을 API 연동으로 바꾸면 그대로 붙습니다. **손님이 실제 날짜를 보는 유일한 화면이라 예약 기능의 선행 조건입니다**
- **v18 이전에 등록된 프로그램은 좌표(`location.lat`/`lng`)가 비어 있습니다.** 자동으로 안 채워져서 그 프로그램에는 날씨가 안 붙습니다 — 건수가 적은 지금은 재등록이 빠르고, 늘기 전에 백필 스크립트를 만드는 편이 낫습니다(19-6)
- **`/provider/apply`의 사진이 Unsplash 임시본입니다.** 우리 사진이 아니고 외부 CDN이라 통제할 수 없습니다 — 오픈 전 실제 사진으로 교체 필요(파일 상단 TODO)
- **`/provider/apply` 문구가 아직 없는 기능을 약속합니다.** 「모집·결제·환불·당일 안내까지 시스템이 처리합니다」는 예약·결제가 붙은 뒤에야 사실입니다. 실제 전문가에게 보여주기 전에 톤 조절 여부를 결정해야 합니다
- `src/types/firestore.ts`가 아직 스키마 v9 기준입니다(v18에서 검색 필터 필드만 보강). 예약·결제·정산 타입이 비어 있습니다
- **프론트엔드에는 테스트 하네스가 없습니다.** `npm test`는 보안규칙과 Cloud Functions만 돌립니다 — v21의 회차 입력 화면(`ScheduleFields.tsx`)도 검증 없이 남습니다(서버 쪽 판정은 `schedules.test.ts`가 덮습니다) — `src/lib/programFilter.ts`처럼 조용히 틀리는 판정 로직(연령 `all` 규칙·가격 「이상」 처리)이 검증되지 않은 채 남습니다
- Firebase Storage — **프로그램 사진은 v25에서 완료**됐습니다. **자격증은 아직 올릴 곳이 없어 관리자가 텍스트만 보고 승인**합니다. 미참조 파일 정리도 없습니다 — 작성 중 프로그램에 사진만 올리고 떠나면 파일이 남습니다(18-7)
- **게시 중인 프로그램의 사진 교체가 심사를 거치지 않습니다.** 사진을 수정본 대기열로 보내면 올려도 화면에 아무것도 안 나타나 고장으로 읽히기 때문입니다. 사진은 심사 대상이므로 **관리자 목록에 「사진이 바뀌었다」를 표시**해야 합니다(코드에 TODO)
  - **자격증 열람을 구현하는 날 IAM부터 확인하세요.** 서명 URL은 함수 기본 서비스 계정으로는 발급되지 않습니다 — `서비스 계정 토큰 생성자` 역할이 필요하고, **코드로는 해결되지 않습니다**(18-6)
- 공급자 심사 목록은 `users`에서 `role=='provider'`를 훑어 만듭니다(5번 v15). 공급자가 수백 명을 넘으면 컬렉션그룹 인덱스로 바꿔야 합니다 — 잘리면 응답의 `truncated`로 화면에 표시됩니다

**타입 주의** `src/types/firestore.ts`는 여전히 스테일하지만, v18에서 **검색 필터가 쓰는 만큼은 채웠습니다** — `targetAgeMin`/`Max`·`targetAgeTags`·`walkingDistanceM`·`difficulty`·`rainAlternative`·`sido`·`ratingAvg`/`ratingCount`(전부 선택 필드). 예약·결제·정산 쪽은 손대지 않았습니다. 그 뒤 추가·변경된 것:
- v10 — `providerProfiles` 공개/`private` 분리, `programs.reviewedBy`, `bookings.unitPrice`·`totalAmount`, `schedules.programId`·`programStatus`
- v11 — `users.authProvider` 2종 축소, `users.identityVerifiedAt`, `users/{uid}/private/identity`
- v12 — `bookings.participants`·`guardian`·`emergencyPhone`
- v13 — `programs`에 필터·정렬 필드 12종(`targetAgeMin`/`Max`·`targetAgeTags`·`difficulty`·`walkingDistanceM`·`rainAlternative`·`sido`·`requiresChildInfo`·`scheduleDates`·`nextScheduleAt`/`lastScheduleAt`·`publishedAt`·`ratingAvg`/`ratingCount`·`bookingCount30d`), `aggregates` 컬렉션 신규, `programs` update 규칙을 허용목록으로 반전
- v14 — 소셜 가입 정책 확정. `users.name` 폴백은 이메일 앞부분이 아니라 `이용자{uid 앞 4자리}`(공개 노출 필드라 이메일이 화면에 뜸), `users.phone`은 양쪽 공급자 모두 사전 심사 필요·심사 후에도 빈 값 가능, 계정 통합 안 함(소셜별 별도 계정), 전화번호 중복은 감지·기록만 하고 차단은 쿠폰 발급 시점

**검색 화면은 서버가 걸러줍니다** (v28). `GET /programs/search`가 필터·정렬·검색어를 판단하고, 화면은 요청과 표시만 합니다. **판정 원본은 `functions/src/lib/programSearch.ts`입니다** — 프론트의 `matchesFilters`는 지웠습니다(두 벌로 두면 「화면에는 보이는데 서버 결과에는 없는」 프로그램이 생깁니다).

> **`status`는 요청에서 받지 않습니다.** 이 경로는 로그인 없이 호출되고 Admin SDK라 규칙을 우회하므로, 받으면 심사 중·반려 프로그램이 그대로 검색에 노출됩니다.
>
> **조건의 원본은 주소(URL)입니다 — 화면이 따로 기억하지 않습니다** (2026-08-28, 17-4 ⑤). 헤더의 「프로그램 찾기」와 「지도로 찾기」는 **같은 화면**이라 눌러도 화면이 새로 만들어지지 않습니다. 예전에는 화면이 조건을 **처음 한 번만** 읽고 기억해서, 찾기 화면에서 「지도로 찾기」를 누르면 **주소와 헤더 강조만 바뀌고 목록은 그대로**였습니다(푸터의 카테고리 링크도 같은 고장이었습니다). **조건을 화면 상태로 되돌리지 마세요.**
>
> **화면 안 조작도 주소를 갱신합니다** — 목록형/지도형·필터 모달·정렬·검색어 전부입니다. 안 그러면 지도를 보는 중인데 헤더는 「프로그램 찾기」가 강조되고, 그 주소를 공유하면 상대는 목록을 봅니다. **조건 하나를 바꿔도 나머지는 주소에 남습니다** — 예전에는 카테고리를 누르면 주소를 새로 만들어 기간·지역이 조용히 사라졌습니다.
>
> **읽고 쓰는 규칙은 `src/lib/programFilter.ts` 한 곳**입니다(`parseSearchScreenParams`·`toScreenParams`). **주소의 필터 이름은 검색 API와 같은 이름**을 씁니다(`priceMin`·`ageTags`·`difficulty`·`barrierFree`) — 스키마 17-4 ⑤의 예시는 `price=0-50000`처럼 짧은 이름이지만 **되돌리지 마세요**(이름이 두 벌이면 한쪽만 고쳐 조건이 조용히 무시됩니다).
>
> **홈에서 고른 지역은 칩으로 보입니다** (2026-08-27). 시·군·구까지 좁힌 조건(`sido`+`locality`)은 **상세 필터의 권역과 층이 달라** 필터 모델에 넣지 않고 따로 들고 다닙니다 — 섞으면 「경상을 골랐는데 사상구가 함께 걸린」 상태를 화면에 표현할 수 없습니다. **칩을 지우면 전국으로 넓어집니다.** 그 지역에 프로그램이 0건일 때는 「어느 지역이 비었는지」를 문장으로 말해줍니다.
>
> **인기순·평점순은 기준 필드가 비어 있는 동안 「최근 게시순」으로 떨어집니다.** 예약 수를 채우는 배치가 없어 값이 전부 0인데, 그대로 두면 순서가 매번 바뀝니다.
>
> **「가까운거리순」만 화면이 정렬합니다** — 서버는 사용자의 현재위치를 모릅니다.
>
> **⚠️ 썸네일이 아직 없습니다.** 목록 카드가 원본 사진(긴 변 1600px)을 씁니다. **게시 프로그램이 20건을 넘거나 오픈 전에는 반드시 넣어야 합니다** — 카드 20장이면 5MB가 나갑니다(20-6).

**홈 화면은 2026-08-26에 개편했습니다.** 구성은 **첫 화면(제목 + 통합 검색 + 카테고리 아이콘 5개) → 인기 프로그램 4장 → 전문가 배너 → 후기**입니다. 참고 시안의 디자인을 따르되 **기능은 우리 것을 씁니다** — 시안의 「전문가 찾기」·「맞춤 견적요청」·「1분 만에 맞춤 견적 신청」은 **없는 기능이라 빼거나 실제로 되는 것(전문가 가입 안내)으로 바꿨습니다.**

> **홈이 서버를 부르는 것은 이번이 처음입니다.** 인기 프로그램은 검색 화면과 **같은 경로**(`GET /programs/search`)를 씁니다 — 판정을 두 벌로 두지 않기 위해서입니다. 불러오는 동안 자리표시를 그리고, 게시된 프로그램이 없으면 안내 문구로 대체합니다(**오류로 첫 화면이 깨지지 않게** 실패해도 빈 목록으로 떨어집니다).
>
> **통합 검색이 고른 값은 주소로 넘어갑니다**(`category`·`region`·`headcount`, **`q`**). 이때 **검색 화면이 이 값들을 주소에서 읽도록 함께 고쳤습니다** — 안 읽으면 홈에서 고른 조건이 **조용히 무시되고 전체 결과가 나옵니다.**
>
> **지역 칸은 전국 시·군·구까지 알아듣습니다** (2026-08-27, 팀 요청). 「사상구」만 쳐도 「부산 사상구」가 나옵니다. 목록만 두면 머릿속에 있는 이름을 칠 수 없고, 직접 입력만 두면 어떤 지역을 다루는지가 화면에서 사라집니다 — 둘 다 둡니다.
>
> **후보는 두 곳에서 옵니다** — **전국 행정구역 목록**(`src/lib/districts.ts`, 시·도 17 + 시·군·구 228)과 **프로그램이 있는 지역**(검색 응답의 `districts`, 읍·면·동까지 + 개수). 프로그램이 있는 곳을 위에 올리고 개수를 붙입니다 — 없는 지역을 골라 0건을 보면 고장으로 읽힙니다.
>
> **⚠️ `districts.ts`는 서버 사본이 아닙니다.** 서버는 이름을 받아 **주소에 들어 있는지**만 보므로, 지역이 통합·신설되면 **이 파일 하나만** 고치면 됩니다(`sido.ts`·`programContent.ts`와 다릅니다). **읍·면·동을 전국 단위로 넣지 마세요** — 3,500개가 되고, 우리가 가진 동 이름은 서버가 공짜로 알려줍니다.
>
> **관광지·건물 이름(「성산일출봉」)은 알아듣지 않습니다.** 카카오 장소검색을 홈에 열어야 하는데 그 경로는 **로그인 필수로 잠근 것**이고(비로그인에 열면 우리 사용량을 남이 씁니다), 홈은 로그인 없이 오는 화면입니다. 그런 글자는 **버리지 않고 검색어(`q`)로** 넘어가 프로그램 이름·소개에서 찾습니다.
>
> **브라우저 기본 자동완성(`datalist`)은 쓰지 않았습니다** — 목록이 열리는 방식이 브라우저마다 다르고(파이어폭스는 눌러도 안 열림) 「여기 목록이 있다」는 표시가 없어, 정작 고치려던 문제가 그대로 남습니다.
>
> **날짜 칸이 있습니다.** 순서는 **지역 → 날짜 → 프로그램 종류 → 참여 인원 → 검색**이고, 검색 화면 필터의 달력을 그대로 씁니다(서버는 손대지 않았습니다). **단계별로 화면을 넘기는 방식으로 바꾸지 마세요** — 지역만 정하는 사람과 날짜만 정하는 사람이 대부분인데, 단계로 만들면 앞 칸을 비운 사람이 다음으로 갈 수 없습니다.
>
> **카테고리 아이콘은 선 아이콘입니다.** 삽화(PNG)를 두 번 시도했다가 되돌렸습니다 — ① 다섯 칸의 그림체·여백·색이 제각각이라 한 줄로 놓으면 정리가 안 되고 ② 배경을 투명하게 처리해도 화면에 따라 어둡게 비쳤습니다. **원본 그림은 저장소 밖(바탕화면 `참고` 폴더)에 있습니다.**
>
> **평점은 후기가 있을 때만 보여줍니다.** 지금은 값이 전부 0이라 그대로 두면 모든 카드에 「★ 0.0 (0)」이 붙습니다(2-3의 같은 규칙).
>
> **⚠️ 홈의 후기 3건은 여전히 지어낸 내용입니다.** 이번 개편에서도 팀 판단으로 남겼습니다 — 위 「현재 상태」의 경고 참고.

**검색 화면 필터 UI** `src/pages/SearchPage.tsx`는 **17-3 기준으로 다시 썼습니다** (v18). 카테고리 다중 선택 + 「필터」 버튼 뒤의 상세 필터 모달(지역·가격·인원·대상연령·난이도·배리어프리·우천대체)이고, 지역은 주소 문자열이 아니라 `sido` 코드로 판정합니다. 판정 규칙은 `src/lib/programFilter.ts`에 순수 함수로 모아뒀습니다 — **검색 API가 생기면 이 규칙이 서버로 옮겨갑니다.**

> **아직 남은 것 하나 — 목록⇄지도가 별도 토글**로 남아 있습니다(17-4 ①은 합치기로 확정). ~~기간(날짜) 필터~~는 **완료했습니다**(2026-08-24) — 모달에 달력이 들어갔고 판정은 서버(`from`/`to`)가 합니다. 상세는 위 「내일 할 일」의 3순위 항목 참고.
>
> **사본이 둘 있습니다 — `src/lib/sido.ts`와 `src/lib/programContent.ts`.** 각각 서버 `functions/src/lib/`의 같은 이름 파일이 기준입니다. 검색 API를 붙일 때까지만 두는 임시본이라 **한쪽만 고치면 결과가 갈립니다.**

**~~mock 주의~~** — `src/mocks/`는 v28에서 삭제했습니다. **이제 화면에 보이는 것은 전부 실제로 등록·승인된 프로그램입니다.** 홈·검색이 비어 보이면 게시된 프로그램이 없다는 뜻입니다(고장이 아닙니다).

---

## 작업할 때

**테스트는 루트에서 `npm test` 하나로 돌립니다.** 보안규칙 84종(Firestore 68 + 파일 16) + 함수 192종이 함께 돕니다.

> **개발용 에뮬레이터를 띄워둔 채로도 돌아갑니다.** `firebase.test.json`이 테스트 전용 포트(18080·19099·15001)를 쓰기 때문입니다 — 이 파일이 없으면 "포트가 이미 쓰이고 있다"며 테스트가 아예 시작되지 않습니다. 개발용 에뮬레이터의 데이터가 테스트 데이터로 오염되는 것도 이 분리가 막아줍니다.

**⚠️ `npm test`에는 화면(`src/`)이 들어 있지 않습니다. 화면 코드를 고치면 `npm run lint`를 반드시 돌리세요** (2026-08-25 신설). 왜 lint만이 화면을 지키는지(훅 순서 오류는 타입 검사·빌드로 못 잡음)와 알려진 경고 6건에 대한 설명은 `MD/frontend/frontend.md`에 있습니다 — **`error`가 0인지만 보면 됩니다.**

- **mock을 실제 API로 바꿀 때** 코드 안 `TODO(백엔드 연동)` 주석이 연동 지점입니다
- **새 컬렉션을 만들면 보안규칙도 같이 추가하세요.** 규칙을 빠뜨리면 보안 사고가 아니라 "조회가 안 되는 버그"로 나타납니다 — 기본이 거부이기 때문입니다
- **스키마를 바꾸면 문서도 같이 고치세요.** 버전을 올리고 변경 이력에 "왜 바꿨는지"를 남기는 게 이 프로젝트 관례입니다
- 커밋 메시지는 `타입: 내용` (예: `feat: 예약 홀드 로직 추가`, `fix: 웹훅 멱등 처리 누락`, `docs: 스키마 v12`)

**테스트 종수는 늘어납니다.** 위 숫자는 작성 시점 기준이라 지금은 더 많습니다 — 정확한 수는 `npm test` 결과를 보세요.

### 작업이 끝나면 현황판도 갱신합니다 (2026-08-25 신설)

**기능 하나를 끝냈거나 상태가 바뀌었으면, 코드·문서와 함께 현황판까지 고쳐야 그 작업이 끝난 것입니다.**

**왜 규칙으로 두는가** — 팀은 코드도 이 문서도 보지 않습니다. **진행 상황을 보는 창구가 현황판 하나뿐**입니다(https://foom7707.github.io/foom-board/). 코드와 문서만 고치고 현황판을 그대로 두면 **팀에게는 아무것도 안 바뀐 것으로 보입니다** — 실제로 다 해놓고 「아직 안 됐다」는 말을 듣게 됩니다.

**어디를 고치나** — `C:\Users\user\Desktop\foom-board\index.html` (**프로젝트 밖입니다.** 저장소가 `FOOM7707/foom-board`로 따로 있어 이 프로젝트와 섞이지 않습니다)

**고칠 곳은 파일 안의 `<script id="state">` 한 군데뿐입니다.** 여기만 고치면 진행률·일정 막대·요약 숫자가 전부 따라 바뀝니다.

| 무엇이 바뀌었나 | 어디를 고치나 |
|---|---|
| 항목을 끝냈다 / 시작했다 / 막혔다 | 그 항목의 `status` — `done`(완료) `wip`(진행 중) `blocked`(막힘) `todo`(미착수) |
| 왜 그 상태인지 설명이 필요하다 | 같은 항목의 `why` — 한 줄. 비워두면 상태만 보이고 이유가 안 보입니다 |
| 계약·심사처럼 개발로 안 풀리는 것이 생겼다 | `external` |
| 커밋 수·테스트 종수가 늘었다 | `totals` — 테스트 종수는 `npm test` 결과가 기준입니다 |
| (매번) | `updated`를 **오늘 날짜로** 바꿉니다. 안 바꾸면 팀이 옛 화면을 보고 최신인 줄 압니다 |

**고친 뒤 그 폴더에서 커밋·푸시하면 1분쯤 뒤 사이트에 반영됩니다.** 프로젝트 저장소와 별개라 `main` 하나만 씁니다 — 위 브랜치 규칙(`main`·`dev`)과는 무관합니다.

**⚠️ 웹 현황판은 「보기 전용」입니다.** 화면에서 상태를 눌러 바꿀 수 없으니 반드시 위 파일을 고쳐 올립니다. `rev`(판 번호)와 `log`(바뀐 기록)는 손대지 않습니다.

---

## 「퇴근」 — 하루 마감 보고

**사용자가 `퇴근`이라고 입력하면 그날 한 일을 정리해 보고합니다.** 세션이 매번 새로 시작되므로 그때마다 무엇을 정리해달라고 설명하지 않아도 되게 규칙으로 둡니다.

### 하는 일 (순서대로)

1. **오늘 무엇이 바뀌었는지 확인** — `git log --since=midnight`, `git status`, `git log origin/dev..dev`
2. **커밋되지 않은 변경이 있으면 먼저 알립니다.** 보고서만 쓰고 넘어가면 다음 세션에서 정체 불명의 변경으로 남습니다. 커밋·병합 여부는 사용자에게 확인받습니다
3. **문서가 어긋났는지 점검** — 위 「현재 상태」·「다음 작업 순서」·「대기 중인 외부 항목」이 실제와 다르면 함께 고칩니다. 이게 다음 세션의 출발점입니다
4. **현황판을 갱신합니다** — 「작업할 때」의 「작업이 끝나면 현황판도 갱신합니다」 그대로입니다. **보고서에 쓸 내용과 현황판에 쓸 내용이 같습니다** — 보고서를 쓰면서 현황판을 안 고치면, 팀은 그 보고서를 받고 화면에 들어갔다가 아무것도 안 바뀐 것을 보게 됩니다. 고쳤으면 **커밋·푸시까지** 하고, 반영됐는지 확인한 뒤 보고서에 주소를 함께 적습니다
5. **보고서를 대화창에 출력합니다** — 파일로 만들지 않습니다(git 이력과 「현재 상태」가 이미 그 역할을 합니다. 세 번째 기록처를 만들면 어긋나기 시작합니다)

### 보고서 형식

```
## YYYY-MM-DD 작업 내용
1. (묶음 제목)      ← 기능 단위로 묶고, 왜 필요했는지 한 줄
2. ...
## 다음 작업        ← 무엇이 막고 있는지 근거와 함께
## 외부 대기 항목    ← 표: 항목 / 없으면 막히는 것
```

### 쓰는 말

**개발 용어를 쓰지 않습니다.** 팀에 그대로 보고할 수 있는 문장이어야 합니다.

| 쓰지 않음 | 대신 |
|---|---|
| 「스키마 v20」 「17-3 기준」 | 버전·문서 번호는 적지 않습니다 |
| 「`ProgramMap.tsx` 교체」 | 「지도를 카카오맵으로 교체」 |
| 「`GET /external/kakao-map/search` 신설」 | 「주소를 검색해 좌표를 받아오는 기능」 |
| 「파생 필드 백필」 | 「이미 등록된 프로그램은 값이 비어 있어 다시 채워야 함」 |

- **무엇이 되고 무엇이 안 되는지**를 분명히 씁니다. 「연동했다」로 끝내지 않고 「실계정으로 로그인까지 확인했다」/「화면은 됐지만 데이터는 아직 가짜다」까지 적습니다
- 못 한 것·막힌 것을 빠뜨리지 않습니다. 대기 항목은 **누가 무엇을 해야 풀리는지**까지 씁니다(예: 「대표님 본인인증이 있어야 신청 가능」)
## 동작 규칙
대규모 코드 수정 전, 작업 계획(Plan)을 먼저 요약해서 제시할 것.