# 품(FOOM)

산림복지전문가와 지역주민을 잇는 프로그램 예약 중개 플랫폼. 운영 주체는 체질숲협동조합.

**스택** Firebase Hosting + Cloud Functions(Blaze) + Firestore / React 19 + TypeScript + Vite + Tailwind v4 + shadcn/ui
**외부 연동** 지도=카카오맵 · 결제=토스페이먼츠+포트원 · 날씨=기상청 공공데이터포털 · 알림=카카오 알림톡(+SMS fallback) · 로그인=카카오·네이버 소셜 (본인확인은 **공급자 등록에만**)

---

## 문서 지도

> **작업을 시작하기 전에 아래 표에서 관련 문서를 찾아 반드시 읽으세요.**
>
> 이 파일에는 모든 작업에 공통으로 적용되는 규칙만 있습니다. **필드명·API 경로·상태값·보안규칙은 여기 없습니다.**
>
> - 필드명이나 API 경로를 **추측해서 코드를 쓰지 마세요.** 반드시 해당 문서를 열어 확인한 뒤 작성합니다
> - 스키마 문서는 900줄이 넘습니다. 통째로 읽지 말고 **표에 적힌 섹션 번호만 찾아 읽으세요**
> - 작업 도중 판단이 서지 않는 결정 사항이 나오면 "검토 결과 판단 근거"에 왜 그렇게 정했는지가 적혀 있습니다. 추측하기 전에 먼저 확인하세요
> - 문서와 코드가 어긋나면 **문서가 기준**입니다. 코드를 문서에 맞추고, 문서가 틀렸다고 판단되면 임의로 고치지 말고 먼저 알려주세요

| 알고 싶은 것 | 읽을 파일 | 섹션 |
|---|---|---|
| Firestore 필드 정의, 컬렉션 구조 | `docs/백엔드-설계-스키마.md` | 2번 |
| API 엔드포인트 목록 | 〃 | 5번 |
| 보안규칙 전문 | 〃 | 6-1 |
| 보안규칙 테스트 케이스 | 〃 | 6-3 |
| 인덱스 | 〃 | 7번 |
| 관리자 페이지 설계 | 〃 | 12번 |
| 착수 전 필수 보안 조치(P0) | 〃 | 13번 |
| 왜 이 스택인가 (벤더 선정 근거·수수료·바우처) | 〃 | 14번 |
| 로그인·회원가입·본인확인 정책 | 〃 | 15번 |
| 날씨 API 연동 (캐싱·예보 기간) | 〃 | 16번 |
| 검색 필터·기간 선택 (필드·쿼리 전략·캘린더 규칙) | 〃 | 17번 |
| 왜 그렇게 정했는지(의사결정 이력) | 〃 | 0번 + 맨 끝 "검토 결과 판단 근거" |
| 컬렉션 관계 한눈에 보기 | `docs/백엔드-구조-다이어그램.html` | — |
| 화면 구성·홈 디자인 기준 (색·레이아웃) | `docs/디자인-웹페이지-초안.html` | — |
| 수수료·환불·정산 관행의 근거, 경쟁 플랫폼 분석 | `docs/벤치마킹-분석-리포트.md` | — |
| 스프린트 일정 | `docs/스프린트-로드맵.md` | — |

경로는 프로젝트 루트 기준 상대경로입니다. **스키마 문서는 파일명에 버전을 넣지 않습니다** — 버전은 문서 상단 제목과 변경 이력으로만 관리하므로, 버전이 올라가도 위 경로는 그대로입니다. 현재 최신은 v14입니다.

---

## 절대 규칙

**돈·권한·상태가 걸린 데이터는 클라이언트가 직접 쓰지 않습니다.**
`bookings` `payments` `settlements` `reviews` `providerProfiles` — 전부 Cloud Functions(Admin SDK) 경유. 보안규칙에서 `allow write: if false`로 막혀 있습니다.

**관리자에게도 클라이언트 쓰기를 열지 않습니다.** (`voucherAds`만 예외)
심사·정산은 반드시 `/admin/*` API를 거칩니다. 감사로그를 남겨야 하기 때문입니다.

**보안규칙은 클라이언트 SDK에만 적용됩니다.** Cloud Functions는 Admin SDK라 규칙을 우회합니다. `/admin/*` 함수는 진입부에서 `context.auth.token.admin`을 직접 검사해야 합니다. 프론트 라우트 가드는 UX 장치일 뿐 보안이 아닙니다.

**결제 금액은 서버가 정합니다.** 클라이언트가 보낸 금액은 무시하고 `bookings.totalAmount`를 씁니다. 웹훅에서 실결제액과 대조하되 **기준은 `bookings.unitPrice`(예약 시점 스냅샷)이지 `programs.price`(현재가)가 아닙니다.**

**`users.role`은 어떤 경로로도 클라이언트가 못 씁니다.** 관리자 지정은 Admin SDK 스크립트로 `users.role`과 Custom Claims(`admin: true`)를 **동시에** 갱신합니다. 하나만 하면 화면이 안 열립니다.

**가입은 소셜 2종(카카오·네이버)뿐입니다.** 이메일·비밀번호 가입은 만들지 않습니다. 로그인 화면에 휴대폰 인증 UI를 넣지 마세요.

**소비자에게는 본인확인을 요구하지 않습니다.** 로그인·예약·결제 어디에도 넣지 않습니다. 본인확인은 **공급자 등록에만** 있습니다(15-1). 결제 시 사용자가 겪는 인증은 카드사 인증이고 PG가 알아서 처리합니다. "문자인증"이나 "결제 시 본인확인"이라는 표현이 옛 문서에 남아 있으면 폐기된 설계입니다.

**아동 안전은 예약 폼으로 확보합니다.** 참가자 이름·연령, 보호자 연락처, 비상 연락처, 특이사항을 `bookings.participants`/`guardian`/`emergencyPhone`에 받습니다. 서버에서 인원수·대상연령 범위를 검증합니다(15-6).

**실명·CI 같은 본인확인 값은 `users` 본문에 저장하지 않습니다.** `users/{uid}/private/identity` 하위 문서에 넣고, CI는 화면에 절대 내려보내지 않습니다. 중복 가입 판별은 서버에서만 합니다.

**검색·필터는 프론트에서 Firestore를 직접 쿼리하지 않습니다.** `GET /programs/search` 하나만 호출합니다. Firestore는 쿼리당 논리합이 30개로 제한되는데 카테고리 다중선택 × 대상연령 다중선택만으로 상한에 닿고, 기간까지 얹으면 불가능합니다. 필터 조합마다 복합 인덱스가 필요해지는 문제도 있습니다(17-1). 이 함수는 로그인 없이 호출되고 Admin SDK라 규칙을 우회하므로 **함수 안에서 `status=='published'`를 하드코딩**해야 합니다.

**파생 필드는 등록 화면에 입력칸을 만들지 않고, 보안규칙에서도 막습니다.** `targetAgeTags` `difficulty` `sido` `requiresChildInfo` `scheduleDates` `nextScheduleAt`/`lastScheduleAt` `publishedAt` `ratingAvg`/`ratingCount` `bookingCount30d`는 서버가 계산해 넣는 사본입니다. **`programs` 수정 규칙은 허용목록(화이트리스트) 방식**이라 이 필드를 클라이언트가 넣으면 수정 전체가 거부됩니다 — 금지목록으로 되돌리지 마세요. 필드를 추가할 때는 ① 누가 계산하는가 ② 클라이언트를 막았는가 ③ null인 문서는 어떻게 되는가 ④ 시간이 지나면 스테일해지는가를 함께 정합니다(17-7).

**회차 날짜를 바꾸는 모든 경로는 `syncProgramScheduleDates`만 호출합니다.** 이 함수는 **프로그램 문서만** 갱신합니다. `aggregates/*`는 `rebuildSearchIndex` 배치만 쓰고, 증분이 아니라 전체 재생성입니다 — 프로그램마다 집계 문서를 고치면 한 문서에 쓰기가 몰려 경합이 생기고 `increment`는 재시도로 값이 부풉니다(17-5).

**`nextScheduleAt`을 인덱스나 부등호 필터에 쓰지 마세요.** Firestore는 인덱스 필드에 값이 없는 문서를 색인하지 않으므로, 회차가 없는 상시모집(`open`) 프로그램이 결과에서 통째로 사라집니다(17-2).

**날씨는 캐싱 없이 호출하지 않습니다.** 기상청 단기예보는 하루 8회만 발표되므로 격자(nx, ny) 단위로 3시간 캐싱합니다. 캐시가 없으면 호출이 방문자 수에 비례해 늘어납니다(16-1). 진행일까지 3~4일이 넘는 회차는 예보가 없으므로 안내 문구로 대체합니다.

**보안규칙을 고치면 테스트를 함께 고칩니다.** 규칙은 눈으로 봐서 맞는지 알 수 없습니다 — 실제로 v9까지의 규칙에 권한 상승 취약점이 있었고 리뷰로는 못 잡았습니다. 스키마 6-3의 케이스 22종을 에뮬레이터로 돌립니다.

---

## 코드 컨벤션

**리포** `FOOM7707/FOOM` (private)

### 브랜치는 `main`과 `dev` 두 개뿐입니다

**새 브랜치를 만들지 마세요.** 작업 영역이 달라도, 세션이 바뀌어도 만들지 않습니다.

| 브랜치 | 역할 |
|---|---|
| `main` | **원본.** 항상 배포 가능한 상태. 문제가 생기면 여기로 되돌립니다 |
| `dev` | **작업용.** 모든 커밋은 여기에 쌓습니다 |

```
dev 에서 작업 → 커밋 → 푸시 → dev를 main에 병합 → 다음 작업도 같은 dev에서 계속
```

세션을 시작할 때 **`dev`에 있는지 먼저 확인**하고, 아니면 `git checkout dev`로 옮긴 뒤 작업합니다. 이미 `dev`가 있으므로 `checkout -b`(새로 만들기)를 쓰면 안 됩니다.

**왜 두 개인가** — 롤백 기준점이 필요하기 때문입니다. `dev`에서 뭔가 잘못돼도 `main`에 마지막으로 정상 동작하던 상태가 그대로 남아 있어야 되돌릴 수 있습니다. 반대로 브랜치가 셋 이상이 되면 어디에 무엇이 있는지 알 수 없게 됩니다 — 실제로 v14 시점까지 브랜치 3개가 한 줄로 쌓여, 이름과 내용이 서로 맞지 않는 상태가 됐습니다.

**병합은 일반 merge로 합니다. squash·rebase 병합을 쓰지 마세요.** squash로 병합하면 `main`과 `dev`의 커밋 이력이 달라져 두 브랜치가 갈라지고, 다음 병합부터 충돌이 납니다.

**병합 뒤에도 `dev`를 지우지 않습니다.** 같은 `dev`를 계속 씁니다.

큰 실험이라 `main`을 위험하게 만들 것 같으면, 브랜치를 임의로 만들지 말고 **먼저 물어보세요.**

**디자인 토큰**
- 브랜드 컬러 `#1F5C43` (포레스트 그린). shadcn/ui `--primary`에 매핑돼 있음
- 폰트 **Pretendard 단독**. 토스 폰트·토스 디자인 시스템은 라이선스상 사용 불가
- 라이트 모드 배경은 순백. 다크 모드는 OS 설정을 따름(토글 없음)
- 푸터는 다크 포레스트(`#0E211A` 계열). 네이비 아님

**카테고리 명칭은 자격유형 공식명칭 5종을 씁니다.**
숲해설 / 유아숲체험 / 산림치유 / 숲길등산 / 단체·기업
※ `숲치유`, `등산·트레킹` 같은 옛 표기가 코드에 남아 있으면 고쳐주세요.

**프로그램 대표 이미지는 `imageUrls[0]`.** 별도 썸네일 필드는 두지 않습니다. 홈 카드에 자동 슬라이드를 넣지 않습니다(첫 화면 로딩 부담).

---

## 현재 상태

**동작하는 것** 홈·검색·상세·등록 4개 화면. 전부 `src/mocks/`의 가짜 데이터로 돌아갑니다.

**프로토타입만 있고 연동 안 된 것**
- 로그인 — 모달 UI만. **네이버는 2026-08-14 검수 통과, 키 발급 완료**(전화번호 필수 제공 포함). 앱 등록 값·콜백 URL은 15-7. 카카오는 비즈 앱 전환 + 동의항목 심사 미신청. **모달에 남아 있는 휴대폰 인증 UI는 제거 대상**(15-5)
- 날씨 — `src/lib/weather.ts`의 결정론적 mock. 실제로는 `GET /external/weather`(Cloud Functions 경유 필수, 기상청은 CORS 차단 + 키 노출 문제) 로 교체
- 지도 — **Leaflet + OpenStreetMap 임시**. 카카오맵 키 나오면 `src/components/ProgramMap.tsx` **한 파일만** 교체하면 되도록 props를 고정해뒀습니다
- 위치 — Geolocation·거리 계산은 실제로 동작

**아예 없는 것** 백엔드 전체, 회원가입·인증 상태 관리, 예약·결제, 공급자 대시보드, 관리자 페이지

**타입 주의** `src/types/firestore.ts`는 스키마 v9 이전 기준이라 스테일합니다. 그 뒤 추가·변경된 것:
- v10 — `providerProfiles` 공개/`private` 분리, `programs.reviewedBy`, `bookings.unitPrice`·`totalAmount`, `schedules.programId`·`programStatus`
- v11 — `users.authProvider` 2종 축소, `users.identityVerifiedAt`, `users/{uid}/private/identity`
- v12 — `bookings.participants`·`guardian`·`emergencyPhone`
- v13 — `programs`에 필터·정렬 필드 12종(`targetAgeMin`/`Max`·`targetAgeTags`·`difficulty`·`walkingDistanceM`·`rainAlternative`·`sido`·`requiresChildInfo`·`scheduleDates`·`nextScheduleAt`/`lastScheduleAt`·`publishedAt`·`ratingAvg`/`ratingCount`·`bookingCount30d`), `aggregates` 컬렉션 신규, `programs` update 규칙을 허용목록으로 반전
- v14 — 소셜 가입 정책 확정. `users.name` 폴백은 이메일 앞부분이 아니라 `이용자{uid 앞 4자리}`(공개 노출 필드라 이메일이 화면에 뜸), `users.phone`은 양쪽 공급자 모두 사전 심사 필요·심사 후에도 빈 값 가능, 계정 통합 안 함(소셜별 별도 계정), 전화번호 중복은 감지·기록만 하고 차단은 쿠폰 발급 시점

**검색 화면 주의** `src/pages/SearchPage.tsx`는 v13 필터 설계 이전 상태입니다 — 카테고리가 단일 선택이고, 정렬이 3종뿐이며(인기순은 실제로 정렬하지 않음), 기간·대상연령·난이도·우천 필터가 없고, 목록⇄지도가 별도 토글로 남아 있습니다. 지역 필터는 `regionOfAddress()`로 주소 문자열을 파싱하는데 실제로는 `programs.region` 필드를 써야 합니다(17-3). 필터를 구현할 때 이 화면을 17번 기준으로 다시 씁니다.

**mock 주의** 카테고리 5종 중 **숲해설에 프로그램이 하나도 없습니다.** 홈에서 그 카드를 누르면 검색 결과가 0건이라 상세 화면 확인이 안 됩니다. 카테고리마다 최소 1개는 있어야 합니다.

---

## 작업할 때

- **mock을 실제 API로 바꿀 때** 코드 안 `TODO(백엔드 연동)` 주석이 연동 지점입니다
- **새 컬렉션을 만들면 보안규칙도 같이 추가하세요.** 규칙을 빠뜨리면 보안 사고가 아니라 "조회가 안 되는 버그"로 나타납니다 — 기본이 거부이기 때문입니다
- **스키마를 바꾸면 문서도 같이 고치세요.** 버전을 올리고 변경 이력에 "왜 바꿨는지"를 남기는 게 이 프로젝트 관례입니다
- 커밋 메시지는 `타입: 내용` (예: `feat: 예약 홀드 로직 추가`, `fix: 웹훅 멱등 처리 누락`, `docs: 스키마 v12`)
