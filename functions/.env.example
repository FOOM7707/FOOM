# functions 런타임 파라미터 (비밀 아님)
#
# 이 파일을 functions/.env 로 복사한 뒤 값을 채우세요.
# .env 는 커밋되지 않습니다.
#
# ※ 비밀값은 여기 넣지 마세요. Client Secret 은 .secret.local(로컬) /
#   Firebase 시크릿(운영)에 둡니다 — .secret.local.example 참고.

# 네이버 로그인 Client ID (공개 값. 프론트에도 VITE_NAVER_CLIENT_ID 로 같은 값이 들어갑니다)
NAVER_CLIENT_ID=
